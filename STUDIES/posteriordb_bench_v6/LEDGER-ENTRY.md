### WP35-POSTERIORDB-BENCH-V6 — full validation of the post-WP33 defaults: 45/51 gates, no frozen chain and funnel tail-mass exact, but the release rule fails on one max-|z| 4.023 passing cell and one `sblrc` process error
- Ordered time: 2026-09-04 on `wt/posteriordb-v6`; preregistration, protocol, harness and predictions committed at `7004b2f` before the first evidence cell; 153 posteriordb cells ran 23:18–00:03 local in strict model/arm/seed order, followed by three funnel cells; analysis immediately after.
- Protocol/config: `STUDIES/posteriordb_bench_v6/{PREREGISTRATION.md,protocol.json}`. The v5 protocol verbatim on posteriordb `28f8d3d`: 17 posteriors, arms owalnuts-da / CmdStan 2.39.0 / nutpie 0.16.8, 4 chains x 1,000/1,000, ArviZ 0.23.4 gates rank R-hat <= 1.01, bulk and tail ESS >= 400 on every reference parameter and zero retained divergences. The oWALNUTS arm is the complete sampler default after WP33: `MomentumSum`, Stan metric regularisation, eight levels, WP24 warmup exhaustion rule and `DEFAULT_CHAIN_RESCUE = restart_from_best`; every `ChainRescueUpdate` recorded. Funnel: 4 x 2,000/20,000 at defaults, exact tail-mass z. Release rule fixed before execution: all 153 records, P1 >=42 gates, P2 no passing max |z| >4, P3 fixed-16 non-`arma11` ESS/gradient vs CmdStan >=0.75, P4 fixed-16 wall/gradient <=1, P6 funnel |z| <=2 every seed.
- Seeds: 90101, 90102, 90103, exact-word verified unused outside numeric substrings in prior artifact floats before freezing.
- Status: all 153 posteriordb records and three funnel cells present. Failures are results: oWALNUTS `sblrc` 90101 subprocess exited nonzero with no stderr/raw JSON; nutpie `lotka_volterra` completed 90101 then its child exited before 90102/90103. None rerun.
- Outcome: **oWALNUTS 45/51 gates vs CmdStan 34 and nutpie 29; release rule not met.** P1 held. P2 failed narrowly: passing `one_comp_mm_elim_abs` 90103 max |z| 4.023 (`V_m` -4.023, `K_m` -4.008), with no rescue in the cell and shifts <0.08 reference SD. P3/P4 unevaluable because `sblrc` leaves 15/16 fixed models complete; observed values on those 15 are 0.848x CmdStan ESS/gradient and 0.825x wall/gradient. P5 (reported, not release gate) failed at 0.996x nutpie ESS/s over 15 complete models. P6 held: funnel z -1.04, +0.25, +1.69. P7 held: no frozen oWALNUTS chain. Against v5's fresh seeds, oWALNUTS is 1.075x ESS/gradient over the 16 complete models, with no model below 0.8x; gates improve `hmm_drive_0` 2/3 -> 3/3, `one_comp` 1/3 -> 3/3, `accel_gp` 0/3 -> 1/3 and lose the `sblrc` process-error cell.
- Rescue telemetry: 30 restarts in 21 cells across nine models (21 log-density, nine step); 26 at transition 99, two at 149, two at 249. `arma11` five and `lotka_volterra` five, all cells pass; `hmm_drive_0` one log-density rescue on every seed and 3/3 gates, carrying the documented mode-hiding caveat. `kidiq`, `earnings`, `diamonds`, `sblrc`, centered Eight Schools and `accel_gp` account for the other 17 events, confirming first-boundary false positives and adding two late transition-249 step rescues. Funnel: four rescues; tail mass exact on every seed, but omega bulk ESS 388/539/356 and seed 90103 has two retained divergences.
- Diagnostics: `artifacts/results-table.md` (all cells, head-to-head, v6/v5, every rescue, funnel, predictions), `artifacts/summary.json`, `artifacts/cells/*.json`, `artifacts/{run-log,checks-log,analyze-log}.txt`, `artifacts/measured_on.json`; raw draws and competitor output ignored.
- Artifacts: `STUDIES/posteriordb_bench_v6/{PREREGISTRATION.md,protocol.json,README.md,LEDGER-ENTRY.md,Cargo.toml,Cargo.lock,src/,run_posteriordb.py,checksums.py,CHECKSUMS.sha256,artifacts/}`.
- Conclusion: supported — the current default has the strongest observed breadth count (45/51), removes the frozen-chain class on these seeds, and remains funnel-tail unbiased. Not supported — admission as the final 0.2 release headline under the frozen rule; a bias-free claim on every passing cell; P5 wall-throughput superiority to nutpie on this shared-machine run. The `one_comp` z miss did not involve rescue, while the full telemetry strengthens the safety concern: density rescue fires broadly on early/late chains and on every fresh-seed `hmm_drive_0` mode draw.
- Next decision: (1) keep v5 as the last breadth headline admitted by its rule and report WP35 beside it; (2) preregister a paired rescue-v2 study — current versus second-window/two-consecutive scoring plus no-rescue control — with `hmm_drive_0`, `kidiq`, `earnings`, `diamonds` and funnel safety rows; (3) diagnose the `sblrc` subprocess exit separately without consuming evidence seeds; (4) continue the `delta` / reverse-coarsening efficiency line independently.
