### WP39B-REVERSE-COARSER-POLICY-V2 — `ZeroWeightBeyondAdaptSelected` removes the step confound on average (adapted `h` 1.011x `stop`) and still loses: geomean 0.878x over 17 posteriordb models, 0.703x on the four targets; at an identical fixed step the untruncated orbit is 0.908x on the targets; the WP34 truncation hypothesis is not supported; no default change
- Ordered time: 2026-09-05 on `main`; kernel variant committed first (`cfb1e93`, research-only, `BuildLeafResult::Built { adapts_step }`, `tests/reverse_coarser_policy.rs`: bit-identical to `ZeroWeightBeyond` at a fixed step, installs a smaller step under adaptation), `PREREGISTRATION.md` / `protocol.json` frozen and committed (`9cb1c02`) before the first cell; 204 cells ran in protocol order without interruption, then the 6 funnel rows, then `analyze`.
- Protocol/config: `STUDIES/reverse_coarser_policy_v2/{PREREGISTRATION.md, protocol.json}`. The `posteriordb_bench_v5` cell protocol (17 posteriors at posteriordb `28f8d3d`, 4 chains x 1,000/1,000, `Init::uniform()`, `Metric::diagonal()`, `Limits::admit_worst_case()`, four threads, BridgeStan 2.9.0 libraries reused from `posteriordb_bench_v6`, gates rank R-hat <= 1.01, bulk/tail ESS >= 400, zero divergences, ArviZ 0.23.4). Four arms: `stop` (shipped), `beyond-adapt` (`ZeroWeightBeyondAdaptSelected`, `Adaptation::default()`), `stop-fixed` and `beyond-fixed` (`StopOrbit` / `ZeroWeightBeyond` at the `stop` cell's median adapted step for the same model and seed, `Adaptation::Custom` with step adaptation off and mass adaptation on). Statistic: min bulk ESS per target call (warmup included), seed medians, ratios.
- Seeds: 92101, 92102, 92103 (grep-verified unused before the freeze). CmdStan cited from v5, not rerun. No deviations.
- Status: preregistered decision study with a registered mechanism comparison; all cells and side checks present.
- Outcome: **rule not met; `StopOrbit` stays the default.** C1 geomean over 17 = **0.878** (rule 1.10), C5 target geomean **0.703** (rule 1.15), C2 worst model centered eight schools **0.347**, `accel_gp` 0.659 (rule 0.90); C3 gates 44/51 vs 44/51 (pass), C4 funnel |z| −0.28/+1.25/+0.10 (pass; `stop` +0.18/−0.99/+0.28). Wins: noncentered eight schools 1.19x, `earnings` 1.07x, `hmm_drive_0` 1.05x. Losses: `sblrc` 0.75x, `kidiq` 0.83x, `arK` 0.90x, `gp_pois_regr` 0.90x. Controls geomean 0.940, healthy-model geomean 0.949, vs CmdStan (healthy) 0.826 → 0.783. **Mechanism: not supported.** M1 `beyond-fixed`/`stop-fixed` on the targets **0.908** (rule 1.15): noncentered eight schools 1.21x, `accel_gp` 0.72x, centered eight schools 0.83x, `gp_pois_regr` 0.95x; M2 controls 0.885 (rule within 1.05), scattered 0.37–1.40 with gates 35/51 vs 42/51.
- Diagnostics: P1 (step within 5 % per model) falsified on five models though the geomean is 1.011 (max |log| 0.153: `diamonds` 1.08, `accel_gp` 1.10, centered eight schools 0.86, `one_comp` and `lotka_volterra` 0.94). P2 falsified (geomean below 1; `gp_pois_regr` below its band). P3 falsified: `stop-fixed`/`stop` geomean **0.397** — with step adaptation off, warmup starts at the final small step on an identity metric and spends 2–8x the gradients, and on `arma11` the fixed step (0.67) without an initial step search never built a retained leaf on either fixed arm (every transition refinement-exhausted). The fixed pair stays paired per model but its control geomeans are uninformative. With the step matched, the untruncated orbit still carries the zero-weight tail (`accel_gp` leaves per orbit 357 → 526, 39 % of built leaves at zero weight; centered eight schools 12.6 → 20.3) and no longer gains the larger step's longer moves elsewhere, so the result is worse than WP39's 0.981. `artifacts/results-table.md`, `artifacts/summary.json`, `artifacts/cells/`, `artifacts/funnel/`.
- Artifacts: `STUDIES/reverse_coarser_policy_v2/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/, run_study.py, checksums.py, CHECKSUMS.sha256, artifacts/}`; kernel `src/kernel.rs` (`ReverseCoarserPolicy::ZeroWeightBeyondAdaptSelected`), `tests/reverse_coarser_policy.rs`, CHANGELOG.
- Conclusion: supported — two independent designs (matched adapted step; identical fixed step) agree that carrying the orbit past a failed reverse check pays only on the noncentered eight schools (about 1.2x) and costs gradients wherever the failure region is large. The reverse-coarsening-as-truncation line (WP34 → WP39 → WP39B) is closed; both policies remain research-only.
- Next decision: no further reverse-coarsening policy work. The per-gradient gap to CmdStan on smooth models (`sblrc`, `kidiq`, `arK`, where reverse-coarser stops end about 1 % of transitions) has a different cause and is the next question. Any future fixed-step design must fix the step after warmup's step search and first mass window, not from the first transition.
