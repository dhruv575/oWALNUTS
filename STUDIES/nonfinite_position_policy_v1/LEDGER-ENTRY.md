### WP38-NONFINITE-POSITION-POLICY-V1 — `RejectLeaf` completes every run the frozen kernel aborts and is bit-identical elsewhere, but the registered health gate fails on the target itself; opt-in not qualified, no default change
- Ordered time: 2026-09-04; preregistration `ca1a5a5`, harness and manifest `2511a32`, 72 one-shot cells run in manifest order, analysis immediately after; no cell rerun.
- Protocol/config: `STUDIES/nonfinite_position_policy_v1/{PREREGISTRATION.md,protocol.json}`. Arms `abort` (frozen `v10` behaviour) and `reject` (`NonfinitePositionPolicy::RejectLeaf`, research-only), otherwise the sampler defaults at baseline `36dd4c6`, initial step 0.5, 4 x 1,000/1,000. Targets: repaired polyscope-canonical-v2 state space (24 seeds 97201–97224), Neal funnel and 100-D Gaussian identity controls (6 seeds 97301–97306 each). All 30 seeds grep-verified fresh.
- Status: 72/72 records, every child exit 0, no timeout.
- Outcome: G1 identity pass 12/12; G2 completion pass 24/24; G3 informativeness pass (`abort` failed seeds 97216 and 97224 with `ErrorKind::Numerical`, "kernel attempted a nonfinite target position"); **G4 health fail 0/24** (minimum bulk ESS 200–382 and maximum R-hat 1.006–1.029 across the nine monitored coordinates, against the registered 400 / 1.01); G5 confinement pass (three rejections total, all in the initial fast warmup phase, none retained); G6 consistency pass 22/22 (bit-identical draws and call counts wherever `abort` completed). Predictions P1, P2, P3, P5 held; P4, P6 failed.
- Conclusion: frozen decision **`NOT_QUALIFIED`**. G4 measured the target rather than the policy: on the 22 seeds where both arms completed the draws are identical, so the failing health numbers belong equally to the frozen kernel; the two `reject`-only cells lie inside the same range. The study establishes that the event is real (2/24 seeds at step 0.5), that `RejectLeaf` converts each abort into a completed run with one or two warmup-only rejected leaves, and that the policy is bit-identical to `Abort` on every cell without the event. It does not establish health noninferiority under the registered rule, and it authorises nothing.
- Artifacts: `evidence/{PROVENANCE.json,process/,records/,logs/,analysis.json,RUN-COMPLETE.json}`, `README.md`, `CHECKSUMS.sha256`.
- Next decision: `Abort` stays the default everywhere; `RejectLeaf` remains research-only and undocumented as supported. A renewed qualification needs a fresh preregistration with a paired health gate (each `reject`-only cell within the completed-pair distribution) or a longer run on this target, fresh seeds, and no other change.
