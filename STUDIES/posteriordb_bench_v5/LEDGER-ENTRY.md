### WP32-POSTERIORDB-BENCH-V5 — the post-hoc default change (`MomentumSum` + Stan regularisation) validated on fresh seeds against CmdStan and nutpie: 42/51 vs 36 and 28, 0.82x CmdStan per gradient on the healthy models (1.07x over 17 with `arma11`), 3.09x nutpie ESS/s, funnel exact; all five predictions held
- Ordered time: 2026-09-03 on `wt/joint-flip`; the default change committed first (`d2b9733`, "DEFAULT CHANGE (post-hoc after WP31)"), then the preregistration (`b72af28`), then 153 posteriordb cells 14:32–15:02 local in one strictly sequential pass and the three funnel cells; analysis immediately after.
- Protocol/config: `STUDIES/posteriordb_bench_v5/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). The `posteriordb_bench_v3` protocol verbatim (17 posteriors `28f8d3d`, 4 chains, 1,000/1,000, v3 gates and ArviZ 0.23.4 estimators, `Init::uniform()` starts, 2,700 s cell timeout) with arms `owalnuts-da` = the shipped defaults after the change (`Tuning::default()` with `DEFAULT_U_TURN_RULE = MomentumSum`, `Adaptation::default()` with `DEFAULT_METRIC_REGULARIZATION = Stan`, `h0 0.5`, depth 10, eight levels, `delta 1`, WP24 warmup rule, adapted diagonal metric, cached initial evaluation, `Limits::admit_worst_case()`, four threads on a non-`STAN_THREADS` BridgeStan 2.9.0 build), `cmdstan` 2.39.0 defaults (cmdstanpy 1.3.0), `nutpie` 0.16.8 defaults — both rerun, not cited. Funnel row: Neal's 10-D funnel 4 x 2,000/20,000 at the sampler defaults, per-seed MCSE z. Compiled models and posteriordb checkout copied from `wt/posteriordb-v4` (not modified there); `accel_gp` compiled here. Predictions fixed before the first cell: P1 DA >= 39/51 gates; P2 geomean min-bulk-ESS/gradient vs CmdStan >= 0.45 over 17; P3 geomean ESS/s vs nutpie >= 1.5; P4 wall/gradient <= 1.0x CmdStan; P5 funnel |z| <= 2 on every seed.
- Seeds: 87101, 87102, 87103 (verified unused). Deviations: none; no restart; nutpie panicked inside its own sampler on `lotka_volterra` on every seed as in v3 (three `timeout_or_crash` cells; nutpie head-to-head over 16 models). Machine otherwise idle.
- Status: preregistered validation of a post-hoc decision; the decision was taken before this study on WP31's evidence and is not made by it. All 156 cells present; no oWALNUTS cell errored, froze, diverged or hit the depth cap outside `diamonds` (246–539 per seed) and one `accel_gp` transition.
- Outcome: **P1 held — 42/51** (CmdStan 36, nutpie 28; v3 on the old defaults 35 / 37 / 31; 12 models 3/3 against CmdStan's 10 and nutpie's 8); **P2 held — 1.069x** CmdStan per gradient over all 17 (0.822x over the 16 without `arma11`, where CmdStan lost two of three seeds to a chain that never left its start, step 0, R-hat 1.5, and oWALNUTS is 71x; v3 0.344x); **P3 held — 3.085x** nutpie ESS/s over 16 (0.841x per gradient); **P4 held — 0.801x** wall per gradient (ESS/s 1.401x CmdStan); **P5 held** — funnel +1.02 / −0.05 / +0.93 (0.0571 / 0.0474 / 0.0578 vs 0.0478), zero divergences, one chain on 87103 at `h` 0.0013 with 933 depth caps (unbiased, not efficient — WP28/WP31 caveat unchanged). DA v5/v3 geomean per gradient **2.01x** at 1.02x the gradients (2.31x ESS/s): earnings 3.28, sblrc 13.9 (0/3 -> 3/3, `h` 0.003 -> 0.10–0.12 = CmdStan's, installed max mass 9.2e4 on every chain), kidiq 1.73, nes2000 1.70, hmm_example 1.59, accel_gp 1.71, centered 1.51, garch 1.28, diamonds 1.23, mesquite 1.19, arK 1.13, one_comp 1.12, arma11 1.02, gp_pois_regr 1.03, lotka_volterra 0.95, noncentered 0.85, hmm_drive_0 134 (v3 drew two second-mode seeds, v5 one); no model below 0.8x. Competitor seed-noise control v5/v3: CmdStan 0.65x (arma11 0.003x, centered eight schools 0.28x; 0.90–1.19x on the other fifteen), nutpie 0.74x. Per model vs CmdStan per gradient: healthy models 0.67–0.95x (earnings 0.67, hmm_example 0.73, gp_pois_regr 0.75, noncentered 0.75, arK 0.81, diamonds 0.84, nes2000 0.85, garch 0.86, sblrc 0.87, lotka_volterra 0.88, mesquite 0.90, kidiq 0.92, hmm_drive_0 0.94, one_comp 0.95), centered 1.18, accel_gp 0.47, arma11 71. Outright wins (gates >=, ESS/grad >, ESS/s >): vs CmdStan `arma11` (3/3 vs 1/3) and the centered eight schools (0/3 both, more ESS at zero divergences vs 43–280); vs nutpie `diamonds` (3/3 vs 0/3), `arma11` (3/3 vs 1/3, nutpie 477–515 divergences), `gp_pois_regr` (3/3 vs 0/3). Gate wins without throughput: noncentered eight schools (3/3 vs nutpie 0/3), gp_pois_regr vs CmdStan (3/3 vs 0/3, 6–22 divergences), sblrc vs CmdStan (3/3 vs 2/3), one_comp vs nutpie (1/3 vs 0/3). ESS/s above CmdStan on 8/17 models, above nutpie on 14/16. Regressions relative to v3: none beyond seed noise (noncentered eight schools 0.85x, lotka_volterra 0.95x). `hmm_drive_0` second-mode draw: oWALNUTS one seed (87102, R-hat 1.35), CmdStan one seed (87101, R-hat 1.22, 414 divergences), nutpie none.
- Diagnostics: `artifacts/results-table.md` (per-model cells, head-to-head, per-seed final steps and depth caps, v5-vs-v3 per model and arm, DA against the v3 competitor medians, funnel table, predictions), `artifacts/summary.json` (per-cell metrics including installed-mass extremes, `u_turn` / `metric_regularization` recorded per cell), `artifacts/cells/*.json`, `artifacts/funnel/*.json`, `artifacts/measured_on.json`, `artifacts/run-log.txt`; raw draws and CmdStan CSVs hashed, uncommitted.
- Artifacts: `STUDIES/posteriordb_bench_v5/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, src/bin/funnel.rs, run_posteriordb.py, checksums.py, .gitignore, CHECKSUMS.sha256, artifacts/{summary.json, results-table.md, cells/, funnel/, measured_on.json, run-log.txt, run-log.err.txt}}`; the default change in `src/sampler.rs` (`DEFAULT_U_TURN_RULE`, `DEFAULT_METRIC_REGULARIZATION`, `Tuning::default()`, `Adaptation::warmup_config`), `tests/sampler_api.rs` pins, README, CHANGELOG, `wiki/release-0.2.0.md`, Python package docs.
- Conclusion: supported — the post-hoc pair holds up on fresh seeds against rerun competitors: the most gates of any arm in any posteriordb study, above both competitors on the same seeds, funnel exact, ESS/s above CmdStan and nutpie, no v3 regression. Not supported: a per-gradient win over CmdStan on ordinary posteriors — the honest figure is 0.82x (0.67–0.95x per healthy model; the 1.07x over 17 is `arma11`'s crawl start hitting CmdStan and nutpie on two seeds each); the kernel-side residual of WP30 remains. The decision was post hoc and is documented as such in the CHANGELOG, README and release notes; this study is its validation, not its justification.
- Next decision: (1) the per-gradient residual is now the kernel (WP30's reverse-coarser stops on refined leaves and the remaining orbit length), not warmup — a preregistered kernel study, not another warmup option; (2) `diamonds` depth caps (246–539 per seed at `h` 0.003–0.005) are the one place the depth-10 cap still binds, unchanged by the pair; (3) the funnel's one-chain step collapse at the defaults (87103 `h` 0.0013) is the WP28 item, still open; (4) the Python extension should follow the sampler defaults in its next build (it configures the facade directly and keeps `Endpoints` / `TowardUnit`, documented).
