[package]
name = "exact-state-space-ground-truth-v1"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
owalnuts = { path = "../.." }
rand = { version = "0.9", features = ["small_rng"] }
rand_distr = "0.5"
