### WP29-POSTERIORDB-BENCH-V4 — the v3 protocol on the eight-level default plus a Stan-regularisation arm: eight levels free, Stan regularisation alone fails the default rule on five lost gates, driver died at 187/204 cells (superseded by WP32)
- Ordered time: 2026-09-02, preregistration committed (`74ad37d`) before the first evidence cell; 187 cells 20:32–21:26 local, one strictly sequential run that died during `lotka_volterra` cmdstan 83102 (see deviations); analysis 2026-09-03 on the cells present.
- Protocol/config: `STUDIES/posteriordb_bench_v4/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). Identical to WP25 (v3): 17 posteriordb posteriors `28f8d3d`, 4 chains, 1,000/1,000, every sampler at its defaults, v3 gates and estimators; differences: harness rebuilt against `src/` after the WP26/27/28 merges so `Tuning::default()` has `max_refinement_levels = 8` (kernel revision v10 unchanged; every cell records the 8), arms owalnuts-da, **owalnuts-da-stanreg** (`Adaptation::Custom(WarmupConfig::new(0.8).with_warmup_exhaustion_rule(DEFAULT_WARMUP_EXHAUSTION).with_metric_regularization(DiagonalMetricRegularization::Stan))`, the WP27 `reg` arm), cmdstan 2.39.0, nutpie 0.16.8 (stan-style control dropped), every model compiled fresh; per-cell fraction of retained transitions at refinement level > 0 and at levels 5–8 recorded.
- Seeds: 83101, 83102, 83103 (verified unused on every branch). Deviations: the driver died after 187 of 204 cells (last cell `lotka_volterra` cmdstan 83101 at 21:25:54; the launching background shell was reported killed at ~21:32 while cmdstan 83102 ran a chain on the `rk45` boundary); not relaunched — superseded by WP32 (v5) — and finalised on the cells present at the coordinator's direction. Missing: `lotka_volterra` cmdstan 83102–83103 and nutpie 83101–83103, all twelve `accel_gp` cells, marked *not run*.
- Status: preregistered; incomplete (187/204); no oWALNUTS cell errored; superseded by WP32.
- Outcome: cells passing all gates — owalnuts-da **39**/48 (12 models 3/3; v3 35/51), owalnuts-da-stanreg 33/48, cmdstan 34/46, nutpie 28/45. **Eight levels are free**: levels 5–8 selected by 0.000 of retained transitions on 15 of 16 measured models and 0.005 on the centered eight schools; any refinement 0.3–8 % (centered eight schools 25–29 %); DA v4/v3 ESS/gradient **0.999** geomean on the 13 models without a seed-pathological draw (CmdStan's own 0.952), 0.515x CmdStan on the 12 non-pathological models (v3 0.498x), **0.694x wall per gradient**, 1.67x nutpie per second (9/15 models). Headline DA vs CmdStan over the 15 complete models is **0.845x** per gradient only because CmdStan crawled on two `arma11` seeds (`h` 6e-8 / 2e-7, R-hat 1.59, min ESS 7 — the WP27 §1b start; 1,296x on that model); 0.500x without it (v3 on the same 14: 0.335x, the gap being `hmm_drive_0`'s mode draw: v3 two second-mode seeds, v4 none, 83.6x). Seed draws: `lotka_volterra` 83102 froze one chain at `h` 3e-5 (996 exhaustions) in both oWALNUTS arms; `arma11` 83103 has a slow chain at `h` 0.016 (3/3 anyway). **Stan regularisation alone**: `sblrc` **7.76x** (`h` 0.10 vs 0.004, 3/3 from 0/3), `arma11` 2.48x, hmm_example 1.75x, geomean **1.158x** DA — but `earnings` **0.35x** (0/3, R-hat 1.02–1.14, 0.12x gradients), `diamonds` 0.71x (2/3), and five models lose gates (`earnings` 3->0, `kidiq` 3->0 at R-hat 1.012–1.019 and 0.47x gradients, `diamonds` 3->2, `hmm_drive_0` 3->2 second-mode chain, `one_comp` 1->0): 33 vs 39 cells. Predictions: P1 (>= 35: 39) and P5 (wall/grad <= 1.0x: 0.694) held; P2 (0.9–1.1x v3's 0.344: 0.845) not held by the letter, held on matched sets; P3 (stanreg >= 2x on sblrc and earnings) half — sblrc 7.76, earnings 0.35; P4 (>= 1.1x, none < 0.8x) not held on earnings and diamonds. Default rule ">= 1.1x geomean, no model < 0.8x, no gate lost": **not met**. Agreement: no oWALNUTS cell flagged; CmdStan two flags and nutpie one, all on failing cells.
- Diagnostics: `artifacts/results-table.md` (per-model table with refined fraction and depth caps, head-to-head, stanreg-vs-DA per model with final steps, v3-vs-v4 per model, refinement table, predictions), `artifacts/summary.json`, `artifacts/cells/*.json` (per-parameter z, per-chain steps/exhaustions, depth and nine-bin refinement histograms, the recorded regularisation), raw draws hashed and uncommitted.
- Artifacts: `STUDIES/posteriordb_bench_v4/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, run_posteriordb.py, checksums.py, CHECKSUMS.sha256, artifacts/{summary.json, results-table.md, cells/, measured_on.json, run-log.txt, run-log.err.txt}}`.
- Conclusion: supported — (a) the WP28 eight-level default costs nothing on the posteriordb set (the extra levels are never selected outside the centered eight schools; the DA arm reproduces v3 within seed noise and passes more cells); (b) `DiagonalMetricRegularization::Stan` fixes the `sblrc` collapse and the `arma11` unit-floor step on the full set, as WP27 measured. Not supported: the regularisation as a default on its own — at the corrected metric the endpoint-U-turn orbit is too short on `earnings`, `kidiq` and `diamonds` (R-hat misses at a fraction of the gradients), so the metric fix needs the orbit fix; that joint candidate is what WP32 (v5) tested. Not decided here: a headline against CmdStan — 15 of 17 models, and CmdStan's own `arma11` crawls make the all-model geomean uninformative this round.
- Next decision: superseded by WP32. What v4 adds to it: the per-model list of where the regularisation alone loses gates (`earnings`, `kidiq`, `diamonds`, `hmm_drive_0`, `one_comp`) is the regression set any joint default must clear; the `lotka_volterra` `rk45`-boundary freeze (83102, both arms) and the `arma11` start that CmdStan also crawls on remain start-rule matters outside warmup.
