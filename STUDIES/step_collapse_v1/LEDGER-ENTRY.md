### WP27-STEP-COLLAPSE-V1 -- the default step collapse is the unit-variance metric floor, the `arma11` crawl is a start CmdStan cannot leave either; Stan's regularisation stays opt-in
- Ordered time: 2026-09-02 on `wt/step-collapse`; diagnosis on the v3 seeds 79101-79103 of `sblrc`, `earnings`, `arma11` with per-transition warmup telemetry and CmdStan `save_warmup` traces from the same starts (not preregistered), then the remedy table and predictions committed (`4546d30`) before the first table cell.
- Protocol/config: `STUDIES/step_collapse_v1/PREREGISTRATION.md`. Seven posteriordb models (`sblrc`, `earnings`, `diamonds`; `arma11`; controls `kidiq`, `mesquite`, `nes2000`), 4 chains, 1,000/1,000, `Sampler` defaults (WP24 warmup rule) plus one of ten arms through `Adaptation::Custom`; CmdStan 2.39.0 defaults from the same constrained starts. Metric: seed median of min bulk ESS per gradient over unconstrained coordinates (ArviZ over constrained parameters for CmdStan; rank-based, transform-invariant); v3 gates.
- Seeds: 80101-80102 (fresh). Deviations: none; every cell ran once (41 min, no failure).
- Status: diagnostic study with preregistered remedy table; no default flipped.
- Outcome: mechanism (a) -- the `v10` diagonal regularisation `(n/(n+5)) var + 5/(n+5)` floors every variance at 0.0099 for `n = 500`; `sblrc`'s coefficients have posterior variance 1.07e-5 (CmdStan's inverse metric), so the installed metric is 1,000x too wide, the momentum scale 30x too large in the stiff directions and dual averaging, whose statistic sits exactly on 0.80 throughout, correctly holds `h` at 0.003 = CmdStan's 0.10 / 30. `earnings`: three of five variances (1.6e-4 to 5e-4) floored to 0.01, `h` 0.003 vs 0.017, 9-14 % depth-cap draws. `diamonds` is not a collapse case at depth 10. Stan's statistic, Stan's `init_stepsize` at every window, the delta ramp, `mu = ln 10h`, a floor at half the search result and a 4x/10x within-stream shrink bound all leave `h` at 0.003-0.007 on `sblrc`; `DiagonalMetricRegularization::Stan` alone gives `h` 0.10-0.12 and 9x ESS/gradient. Mechanism (b) -- `arma11` 79103 chain 3 (start log density -3.1e84) slides under the WP24 rule to energy 3.7e5 by transition 250 and then crawls at `h` 1e-8 (statistic on target, 700-leaf orbits, position moving 1e-6 per transition); CmdStan from the same start reaches -3.67e5 at iteration 250 and ends at `h` 1.7e-6 with 832 treedepth-10 draws, R-hat 1.6, ESS 7 -- the same cell. No warmup option moves it (ramp bit-identical; statistic/regularisation/preset end at 5e-8 to 2e-6); a step floor pins the sliding chains. (c) -- `diagnostics::ChainDisagreement` (leave-one-chain-out max R-hat, the chains whose removal alone brings it under 1.01) added to `Summary`. Table (seed-median x baseline): `reg` sblrc 9.7x (gates 1/2, R-hat 1.011), earnings 1.9x but gates 0/2 (min bulk ESS 164-179, R-hat 1.02: at CmdStan's step and metric the WALNUTS orbit is 49 leaves against NUTS's 163, 70 % recursive U-turns, per-draw ESS a quarter of CmdStan's), diamonds 0.90x, arma11 2.9x, controls 1.08x geomean (kidiq 1.06, mesquite 0.97, nes2000 1.24); `reg+research` 3.0x on the collapsing three, 0.98x controls, earnings still 0/2; `stan-style` 1.32x overall, 0.91x controls; non-metric arms 0.72-1.09x with `h` unchanged on the collapsed models; `shrink:10` freezes one `arma11` chain per seed (negative control held). Predictions: P2, P4, P5 held; P1 and P3 half (sblrc yes, earnings 1.9x/0.48x CmdStan).
- Diagnostics: `artifacts/results-table.md`, `artifacts/summary.json`, `artifacts/cells/*.json` (table cells and `diag-*` diagnosis cells, traces stripped), `artifacts/cmdstan-cells/*.json`, `artifacts/table-run.log`; `trace_summary.py` reproduces the per-window tables of README section 1 from a regenerated cell.
- Artifacts: `STUDIES/step_collapse_v1/{README.md, PREREGISTRATION.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, build_models.py, cmdstan_trace.py, trace_summary.py, run_cell.sh, run_table.py, artifacts/}`; `src/walnutpie.rs` (`WarmupConfig::with_step_floor_relative_to_search`, `with_max_window_shrink`, opt-in), `src/diagnostics.rs` (`ChainDisagreement`, `Summary::chain_disagreement`, `RHAT_DISAGREEMENT_THRESHOLD`).
- Conclusion: supported -- (a) is the metric regularisation's absolute floor, not the step rule, the search, the schedule or a chain asymmetry; Stan's regularisation with the WP24 rule removes it on `sblrc` and lifts `earnings` to CmdStan's step, but `earnings` then fails R-hat through a retained-kernel orbit-length gap at that metric, so the preregistered flip rule is not met and the option stays opt-in (recommended for regressions with coefficient scales below ~0.1). (b) is a start both samplers crawl on. Not supported: any step-side remedy for (a); any floor on the warmup step (incompatible with the WP24 escape).
- Next decision: the retained-kernel orbit length under a well-scaled metric on correlated regressions (`earnings` under `reg`: 49 vs 163 leaves at the same `h`) is the blocker for making Stan's regularisation the default and the largest remaining kernel-side gap; a scale-free regularisation (toward the window's median variance) as an alternative default candidate; a cross-chain restart or an init bound for the `arma11`-type start.
