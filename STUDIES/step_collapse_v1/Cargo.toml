[package]
name = "step-collapse-v1"
version = "0.1.0"
edition = "2024"
publish = false
description = "Step-collapse study driver: one posteriordb cell with per-transition warmup telemetry and opt-in remedies"

[dependencies]
owalnuts = { path = "../.." }
owalnuts-bridgestan = { path = "../../integrations/bridgestan" }
serde_json = "1"

[workspace]
