{
 "schema": "owalnuts-funnel-defaults-protocol/v1",
 "study": "funnel_defaults_v1",
 "ledger": "WP28-FUNNEL-DEFAULTS-V1",
 "frozen": "2026-09-02 (before the first evidence cell)",
 "kernel_revision": "walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v10",
 "paper_adaptation_revision": "walnutpie-paper-adaptation-kquantile-gamma-v4",
 "seeds": [82101, 82102, 82103],
 "chains": 4,
 "threads": 4,
 "base": {
  "tuning": {"step_size": 0.5, "max_depth": 10, "min_micro_steps": 1, "max_refinement_levels": 4, "max_error": 1.0},
  "adaptation": "DualAveraging { target_accept: 0.8 } with DEFAULT_WARMUP_EXHAUSTION",
  "metric": "adapted diagonal (Sampler default)",
  "cache_initial_evaluation": true,
  "limits": "admit worst case (Sampler default)"
 },
 "arms": {
  "defaults": {},
  "levels8": {"max_refinement_levels": 8},
  "delta0.5": {"max_error": 0.5},
  "delta0.25": {"max_error": 0.25},
  "levels8+delta0.5": {"max_refinement_levels": 8, "max_error": 0.5},
  "paper-4": {"adaptation": "Paper(PaperAdaptationConfig::default())"},
  "paper-8": {"adaptation": "Paper(PaperAdaptationConfig::default())", "max_refinement_levels": 8},
  "stan-style": {"adaptation": "Custom(WarmupConfig::stan_style(0.8))"},
  "nuts-1": {"max_refinement_levels": 1, "role": "control"}
 },
 "targets": {
  "funnel": {
   "definition": "omega ~ N(0, 9); x_i | omega ~ N(0, exp(omega)), i = 1..9 (arXiv 2506.18746 eq. 32)",
   "dimension": 10,
   "starts": "omega in {-3, -1, 1, 3}, x = 0",
   "warmup": 2000,
   "draws": 20000,
   "exact": {"tail_mass_below_minus_5": 0.0477903522728147, "omega_variance": 9.0}
  },
  "eight": {
   "definition": "noncentered Eight Schools (mu, log tau, z[8]) of STUDIES/eight_schools_v9_rebench_v1",
   "dimension": 10,
   "starts": "log tau in {-2, -1, 0, 1}, others 0",
   "warmup": 1000,
   "draws": 1000
  },
  "gauss100": {
   "definition": "100-D standard Gaussian",
   "dimension": 100,
   "starts": "Init::uniform() (uniform(-2, 2), seeded)",
   "warmup": 1000,
   "draws": 1000
  }
 },
 "measurements": {
  "tail_mass_mcse": "diagnostics::mcse_mean on the indicator omega < -5 (sd / sqrt(ESS_mean))",
  "z": "(estimate - exact) / mcse, per seed",
  "cost": "mean bulk ESS over coordinates / retained target calls, seed median, ratio to the defaults arm"
 },
 "decision_rule": {
  "unbiased": "|z| <= 2 on all three seeds (nuts-1 excluded as a control)",
  "cheap": ">= 0.9x defaults ESS/call on both eight and gauss100",
  "choice": "unbiased and cheap with the fewest seed-median funnel retained calls; else unbiased with the fewest; else none (README note + Tuning::funnel() preset)"
 }
}
