[package]
name = "funnel-defaults-v1"
version = "0.1.0"
edition = "2024"
publish = false
description = "funnel_defaults_v1 study driver: Neal's funnel tail mass at candidate sampler defaults, with Eight Schools and 100-D Gaussian cost cells"

[dependencies]
owalnuts = { path = "../.." }
serde_json = "1"

[workspace]
