[package]
name = "invalid-evaluation-parity-stock-watson"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
owalnuts = { path = "../../.." }
serde_json = "1"
rand = { version = "0.9", features = ["small_rng"] }
rand_distr = "0.5"

[workspace]

[profile.release]
opt-level = 3
