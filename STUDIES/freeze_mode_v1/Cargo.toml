[package]
name = "freeze-mode-v1"
version = "0.1.0"
edition = "2024"
publish = false
description = "Freeze-mode study driver: one posteriordb cell from the v2 starts with per-transition telemetry"

[dependencies]
owalnuts = { path = "../.." }
owalnuts-bridgestan = { path = "../../integrations/bridgestan" }
serde_json = "1"

[workspace]
