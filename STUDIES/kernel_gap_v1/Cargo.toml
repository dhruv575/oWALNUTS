[package]
name = "kernel-gap-v1"
version = "0.1.0"
edition = "2024"
publish = false
description = "Kernel-gap study driver: the oWALNUTS kernel and a reference NUTS on a BridgeStan model at CmdStan's adapted step, metric and post-warmup starts"

[dependencies]
owalnuts = { path = "../.." }
owalnuts-bridgestan = { path = "../../integrations/bridgestan" }
serde_json = "1"
rand = { version = "0.9", features = ["small_rng"] }
rand_distr = "0.5"

[workspace]
