### WP34-REFINEMENT-ROLE-V1 — the paper's promise is not realisable on posteriordb: the adapted macro step already equals Stan's step (geomean ratio 1.000), refinement engages on 0.22% of leaves, and the leaf-error distribution has no stiff subpopulation to absorb, so no arm reaches the flip rule and the defaults stand
- Ordered time: 2026-09-03/04 on `wt/refinement-role`; phase-1 instrumentation (17 cells at the v5 seed 87101, all reproducing v5 bit-for-bit) run first, `PREREGISTRATION.md` and `protocol.json` frozen and committed (`4852fda`) after it and before the first phase-2 cell; the 408 phase-2 cells ran to `run exit 0`; the session hit its usage limit before the funnel and Eight Schools side checks and before the write-up, which was completed from the committed cells by the coordinator.
- Protocol/config: `STUDIES/refinement_role_v1/{PREREGISTRATION.md, protocol.json}`. The `posteriordb_bench_v5` cell protocol verbatim (17 posteriors at posteriordb `28f8d3d`, 4 chains x 1,000/1,000, `Init::uniform()`, `Metric::diagonal()`, `Limits::admit_worst_case()`, four threads, non-`STAN_THREADS` BridgeStan 2.9.0 compiled in this worktree, gates rank R-hat <= 1.01 with bulk and tail ESS >= 400 and zero divergences, ArviZ 0.23.4). Eight arms, no per-model tuning and no source change: `da` (= `Adaptation::default()` + `Tuning::default()`), `da-d2`, `da06`, `da06-d05`, `da06-d2`, `paper08`, `paper06`, `stanacc`. Every cell also ran a level-0 leaf-error trace at multiples of the adapted step (excluded from walls and gradient counts).
- Seeds: 89101, 89102, 89103 (grep-verified unused before the freeze). CmdStan cited from `posteriordb_bench_v5` (seeds 87101–87103), not rerun. Deviations: the funnel and Eight Schools side checks (C4, C5) were not run, so the rule could only be evaluated on C1–C3; no arm reached C1, so the decision is unaffected.
- Status: preregistered decision study; all 408 posteriordb cells present; side checks incomplete.
- Outcome: **no arm meets the rule; the shipped defaults stand.** Instrumentation: geomean adapted-`h` / CmdStan-step ratio **1.000** (0.79–1.23; 0.87–1.12 on the fourteen healthy models); median refined-leaf fraction **0.0022**; gradients per built leaf 1.00–1.09; the trace shows one smooth error population (q99/q50 of `|dH|` at `h` about 10 on every healthy model) with `P(|dH| > 1)` 0.4–6% at `h`, 12–27% at 1.5h, 28–62% at 2h, 60–97% at 3h. Every step-raising arm loses per gradient (`da06` 0.834x `da`, `paper06` 0.604x, `da06-d05` 0.630x, `stanacc` 0.670x). The near-miss is `delta = 2` at the shipped step (`da-d2`): **1.070x** over 17 models, **1.077x** healthy, **0.915x** CmdStan against `da`'s 0.850x, the same 42 gates, step 0.98x, refinement effectively off (0.0% of leaves, 0.3% reverse-coarser stops); `da06-d2` reaches 1.122x healthy at 40 gates. Both fail the no-model-below-0.85x clause on `accel_gp` (0.68x, 0.22x) — the model with a 54% reverse-coarser stop rate, where refinement is doing real work. The four models furthest behind CmdStan in v5 (noncentered eight schools 0.75x, `gp_pois_regr` 0.75x, centered eight schools, `accel_gp` 0.47x) are exactly those with the highest reverse-coarser stop fractions (10%, 20%, 21%, 54%), so the residual is refinement's reverse-coarsening cost, not the step.
- Diagnostics: `artifacts/results-table.md` (instrumentation table, the per-step leaf-error trace per model, per-arm per-model seed medians, decision table), `artifacts/summary.json` (per-cell metrics, per-arm decision statistics, instrumentation, stale side-check slots), `artifacts/cells/` (408 cells), `artifacts/instrumentation/`, run logs.
- Artifacts: `STUDIES/refinement_role_v1/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/, run_refinement.py, checksums.py, artifacts/}`.
- Conclusion: supported — on posteriors whose stiffness is uniform, WALNUTS reduces to NUTS with the adaptive machinery as overhead; its advantage requires *localised* stiffness (Neal's funnel, the T=1000 state-space path), which is a statement about the target class rather than a defect. The per-gradient residual against CmdStan on the hard models is the reverse-coarsening check, now the named target.
- Next decision: (1) a preregistered `delta = 2` decision with the `accel_gp` clause reconsidered, or `delta` adapted per target from the observed `|dH|` distribution instead of fixed; (2) a cheaper reverse-coarsening check; (3) rerun the funnel and Eight Schools side checks before any `delta` flip.
