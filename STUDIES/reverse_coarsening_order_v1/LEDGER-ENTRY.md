# LEDGER-ENTRY — WP37B-REVERSE-COARSENING-ORDER-V1 (paused incomplete, archived)

### WP37B-REVERSE-COARSENING-ORDER-V1 — paused incomplete after the first state-space cell failed; partial exactly-once record archived; no candidate qualification and no default change
- Ordered time: 2026-09-04 on `wt/reverse-coarsening-order`; preregistration `84a76b1`, `4602337`, `c10a253`, `7d08d0f`; implementation/harness `9513ea6`; frozen provenance `09fba0d` (binary SHA-256 `1f111003064b715422469b9992fdd1d9677156ae67f3218b3e82149d27cc6116`); evidence launched 2026-09-04 ~17:00–17:32 local in manifest order and stopped by the unchanged runner at the first failed process; this archival record written the same day without launching, rerunning, replacing or deleting any cell.
- Protocol/config: `PREREGISTRATION.md`, `protocol.json`, `AMENDMENT-1..3.md`. Fixed `delta = 1`; incumbent `FinestToCoarsest` versus research candidate `CoarsestToFinest` reverse-check order over identical generated levels; seven targets (accel_gp, gp_pois_regr, centered and noncentered Eight Schools, 10-D Neal funnel, 100-D Gaussian, polyscope-canonical-v2 state space at T=1000), seeds 96101–96106, two arms, 84 one-shot cells. Initial step 0.5, max depth 10, eight refinement levels, divergence threshold 1000, diagonal metric, default adaptation, 1,000/1,000 transitions, worst-case admission. A pass could qualify only an explicit Rust opt-in; no generic, `walnutpie`, `sampler` or Python default was eligible.
- Status: **paused and incomplete.** 73 launch markers and 73 process records; 72 authenticated raw records covering every cell of the four posteriordb targets, the funnel and the Gaussian (36 complete paired blocks, 72 complete pair-phase units); one failed process; 11 state-space cells never launched.
- Failure: manifest ordinal 72, `state_space_sspd11_t1000/96101/finest_to_coarsest`, exit 1 after 23.134 s, stderr 235 bytes, no raw result: `target evaluation failed` on chain 1, transition 1, target source `Fatal: canonical log density or gradient is not representable as finite f64`. The incumbent arm and the registered target failed, so the cell is evidence neither for nor against coarsest-first ordering. Follow-up diagnostic (`STUDIES/sspd_target_fatal_diag_v1`, diagnostic seeds only) reproduces the class at every diagnostic seed at initial step 0.5 and attributes it to the target's error classification plus a kernel nonfinite-position guard, not to the reverse-check order.
- Gates: M1 completeness/authentication false; M2, M3, W1, W2, W3 unevaluable. Verdict `KEEP_FINEST_TO_COARSEST`. Descriptive only, over the 36 complete paired blocks and not a gate: every pair passed identity and health; candidate reverse calls 5,161,651 versus incumbent 4,754,676 (ratio 1.0856), gated kernel calls 57,721,550 versus 57,314,575 (ratio 1.0071). On these completed targets the coarsest-first order did **not** reduce reverse work.
- Artifacts: committed `evidence/{launch-markers,process,raw,logs,analysis.json,ARTIFACTS-MANIFEST.sha256}`; `evidence/artifacts/` (522 files, 119 GB of per-cell telemetry including the failed cell's partial `.tmp` traces) is Git-ignored and remains immutable on the study machine, with the SHA-256 and byte size of every file in `ARTIFACTS-MANIFEST.sha256`. `CHECKSUMS.sha256` covers every committed study file. `study.py` is unchanged and its `analyze` command still requires `RUN-COMPLETE.json`, which does not exist; `analysis.json` is the failure-analysis record written at the stop.
- Conclusion: the candidate is not qualified; the current finest-to-coarsest behaviour and every default remain unchanged; the 72 completed cells are archived as a labelled partial record and are not interpreted as a selected subset.
- Next decision: any renewed reverse-coarsening decision needs a fresh preregistration and fresh seeds, and must either remove the state-space target or use a repaired, pre-qualified copy of it. The partial descriptive reverse-call ratio (1.086 against the candidate) lowers the prior that coarsest-first is cheaper.
