[package]
name = "posteriordb-bench-v1"
version = "0.1.0"
edition = "2024"
publish = false
description = "oWALNUTS arms of the preregistered posteriordb benchmark (BridgeStan target)"

[dependencies]
owalnuts = { path = "../.." }
owalnuts-bridgestan = { path = "../../integrations/bridgestan" }
serde_json = "1"
rand = { version = "0.9", features = ["small_rng"] }

[workspace]
