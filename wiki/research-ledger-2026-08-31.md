# oWALNUTS research ledger (2026-08-31 program)

Entries follow the Polyscope ledger template. Newest last.

### WP3-1 — outer-selection-bps-vs-multinomial-v1 (reverse ablation of the outer join)
- Ordered time: 2026-08-31T06:22Z (analysis completion; sampling seconds earlier).
- Protocol/config: `STUDIES/outer_selection_bps_vs_multinomial_v1/protocol.json`
  SHA-256 `1ef8074f2a5fc1f3003003e0da1287247245a99b3314e42228cd3d77f6c6c3d1`;
  sampler revision `walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v8`
  (`src/walnutpie.rs` `3e8af684…6e02ac`, `src/kernel.rs` `c0cdec7b…5de1d46`);
  exact v38 noncentered Eight Schools density and four frozen starts; 4
  sequential chains; 1,000 discarded / 1,000 retained; initial step 0.3, depth
  8, refinement levels 8, `max_error` 1.0, divergence threshold 1,000; target
  acceptance 0.95 with dual-averaged step and Welford diagonal mass; arms differ
  only in `OuterOrbitSelection::{BiasedProgressive, ExactNormalizedMultinomial}`;
  10,000,000-call and 120 s caps per cell.
- Seeds: 82001–82003 consumed (both arms, identical starts); no reserved seeds.
- Status: exploratory sampling evidence (preregistered; three seeds; not
  confirmation).
- Outcome: **pass** — primary ratio BPS/multinomial of bulk ESS per retained
  target call (geomean over six functionals) = **1.7453** ≥ 1.10; all safety
  gates held (zero retained divergences/invalid stops in all 24 chains; depth-cap
  delta 0.000; min tail-ESS/call ratio 1.4387; min squared-functional
  bulk-ESS/call ratio 1.6398). All six cells passed health gates.
- Diagnostics: BPS max rank R-hat 1.00303/1.00160/1.00226, min bulk ESS
  2328/2100/2079, min tail ESS 1628/1552/1192, retained calls
  61,871/49,253/63,986, wall 0.099/0.085/0.107 s. Multinomial max R-hat
  1.00344/1.00629/1.00547, min bulk ESS 1139/1143/1244, min tail ESS
  670/1247/991, retained calls 51,945/51,445/62,794, wall 0.087/0.083/0.097 s.
  Self-retention 0.39% (BPS) vs 10.74% (multinomial); lag-1 ACF lower by
  ≈0.3 on every functional under BPS; mean depth 3.77 vs 3.67, leaves built
  13.46 vs 12.62, calls/transition 14.59 vs 13.85; E-BFMI 0.856–1.014 vs
  0.836–1.052. Zero maximum-depth stops in every cell.
- Artifacts: `artifacts/summary.json` SHA-256
  `ecede7fec16818b38b458efc12e4d18f9c81b4098eb19329e411f3dcf98a4c01`;
  runner `src/main.rs` `317a54a1…f63170`; raw cells and `RESULTS.md` pinned in
  `CHECKSUMS.sha256`.
- Conclusion: the biased-progressive outer join is a material efficiency lever
  (≈43% more bulk ESS and ≈30% more tail ESS per gradient on this target) and
  the production default stands. The mechanism is selection (near-zero
  self-retention, reduced lag-1 autocorrelation), not trajectory length.
  Claims not supported: this does not explain the NextStat public-API gap
  (oWALNUTS already uses BPS); no cross-target generality; no source change.
- Next decision: freeze; close the "progressive selection" clue from the
  clean-room study; remaining NextStat decomposition items are adaptation,
  density specialization, and random-start sensitivity.

### WP2-FUNNEL-REPRO-V1 — paper Neal's-funnel reproduction at fixed paper tuning
- Ordered time: 2026-08-31, approximately 02:20–02:45 local (UTC−4); artifacts carry file
  mtimes; preflight preceded sampling.
- Protocol/config: `STUDIES/paper_funnel_reproduction_v1/protocol.json` and
  `PREREGISTRATION.md` (frozen before sampling; hashes in `CHECKSUMS.sha256`).
  Target: 10-D Neal funnel, arXiv 2506.18746 eq. (32). Primary arm F: oWALNUTS
  `KernelTuning(h=0.36, depth 10, min micro 1, 10 refinement levels, δ=0.21)`,
  identity `DiagonalMass`, no `WarmupConfig`, 4 chains from ω ∈ {−3,−1,1,3},
  2,000 discarded + 10,000 retained; F50 = 50,000 retained; N11 = no refinement,
  h=0.11, no local cap (fixed-step NUTS control); N36 = no refinement at h=0.36,
  δ=0.21. Reference arms R0/R1: Flatiron `walnutpie` 0.0.3 PyPI wheel via
  `walnuts_pyfunc`, same δ, 9 halvings, 10 doublings, identity inverse metric,
  R0 zero warmup (its own step search gives per-chain steps 0.127/0.36/1.018/1.018),
  R1 as shipped with 1,000 adaptive warmup. Runtime callback budget
  `min(1e9, exact worst case)`; wall cap 1,200 s per arm. Binary built from the
  working tree while WP1 was editing `src/`; post-hoc arm FT (same seed, rebuilt
  binary) is bit-identical to F, so the fixed-kernel path did not change.
- Seeds: consumed base seeds 81001 (F), 81101 (F50), 81201 (N11), 81301 (N36);
  reference seeds 81001 (R0, R1). Post-hoc: 81001 (FT), 81401 (G), 81501 (S),
  81601 (M8), reference single-chain seeds 81001, 81005, 81006, 81009 (R36;
  81002–81004, 81007, 81008 tried and discarded because their step search did
  not land on 0.36).
- Status: confirmed sampling evidence for the preregistered arms; post-hoc arms
  are exploratory mechanism probes, not evidence.
- Outcome: **paper-claim gates failed for oWALNUTS; passed for the reference.**
  F: P(ω<−5)=0.0959 vs exact 0.0478 (±0.0175), P(ω<−6)=0.0509 vs 0.0228
  (±0.0122), var(ω)=11.06, health clean. F50: 0.0971 ± 0.0090 (z≈10.7),
  0.0557 ± 0.0063, var 11.41, 17 retained refinement exhaustions and 64 depth
  caps in 200,000 transitions; convergence gates pass (R-hat 1.0053, bulk/tail
  ESS ω 1,162/2,147). N11 reproduces the paper's NUTS failure (0.0163, ω never
  below −5.7). N36: 77% refinement exhaustion, R-hat 1.43. R0/R1 tail mass
  within interval. Post-hoc R36 (reference at exactly h=0.36, identity metric,
  4×30,000): P(ω<−5)=0.0477 (z −0.01), P(ω<−6)=0.0217 (z −0.25), 1% quantile
  −6.978 vs exact −6.979, var 8.87, R-hat 1.0042. Post-hoc G (10-D N(0,I), same
  tuning): exact (all |z|<1.3, tails 0.0213–0.0245 vs 0.0228). Post-hoc S
  (h=0.005625, no refinement, depth 14): 0.0350 (z −1.4), 0.0137 (z −1.4).
  Post-hoc M8 (min micro 8, 7 levels): chains trapped below ω=−8 with 99%
  exhaustion, R-hat 1.19.
- Diagnostics: F retained stop mix outer U-turn 22,677 / recursive 3,419 /
  reverse-coarser-accepted 13,904 (34.8%); reverse-coarser stops 31–42% even
  for ω>0 where mean selected level <0.5. Trace kinetics: escape
  P(ω′≥−5|ω<−5)=0.081, entry P(ω′<−5|ω≥−5)=0.0086 (implied neck mass 0.096 =
  observed); arm S entry 0.0031 (implied 0.035). Target calls: F 2,858,238 in
  1.3 s; F50 12,770,697 in 7.4 s; N11 4,430,890; R36 9,939,052 (Python
  callback wall 103.7 s, not a throughput measurement).
- Artifacts: `STUDIES/paper_funnel_reproduction_v1/artifacts/{preflight,F,F50,N11,N36,R0,R1,summary}.json`,
  `artifacts/posthoc-{FT,G,S,M8,R36}.json`, `artifacts/posthoc-summary.json`,
  `artifacts/posthoc-R36-summary.json`; every SHA-256 in
  `STUDIES/paper_funnel_reproduction_v1/CHECKSUMS.sha256`.
- Conclusion: **oWALNUTS's fixed kernel is biased on Neal's funnel at the
  paper's tuning — it places about twice the correct mass in the neck — while
  the reference implementation at identical tuning is exact.** The bias is
  absent on a Gaussian and absent when refinement is disabled, so it lives in
  the refinement/reverse-check path, not in orbit construction or selection.
  Claims not supported: any statement that oWALNUTS reproduces the paper's
  funnel result; any funnel efficiency comparison using the current kernel.
  The paper's NUTS-side claim is supported (N11).
- Next decision: **stop** funnel tuning/adaptation work on the current kernel;
  open a defect hunt in the refinement/reverse-check path. Concrete first
  steps: (1) unit-test that no reverse check runs at level 0 (`num_steps ==
  1` returns reversible in the reference) and count reverse checks per level;
  (2) extend the walnutpie oracle fixtures with funnel-like leaves (large
  forward level, flat reverse region, and the exhaustion-then-stop case) and
  compare leaf acceptance bit-for-bit against `macro_step`/`reversible` in
  `walnutpie-f5bba365/include/walnutpie/walnuts.hpp`; (3) rerun arm F as the
  regression gate (P(ω<−5) within ±0.0090 of 0.0478 at 4×50,000). WP1's
  adaptive warmup must not be evaluated on the funnel until this is fixed.

### WP4-ESSGT-V1 — exact state-space ground truth versus fixed path metrics
- Ordered time: preregistered and executed 2026-08-31T06:0x–06:35Z (UTC); byte-identical re-execution at 06:33Z.
- Protocol/config: `STUDIES/exact_state_space_ground_truth_v1/protocol.json` SHA-256 `2fda8c213c3cbc67c70b7b0292840ffe386e276aee6c923d0371f984897ae928`; sampler commit `bc49ffb` (`walnutpie.rs` `1b1bcbc5…`, built from a git-archive snapshot because the live `src/` was mid-edit by WP1); centered Gaussian local-level path, globals fixed, exact tridiagonal posterior; arms I/D/P/Q (identity, posterior-variance diagonal, posterior-precision bidiagonal Cholesky, prior-precision only); depth 8, refinement 3, max error 1.0, dual-averaged step at 0.8, mass adaptation off, 500/2,000, 4 chains, prior-dispersed starts; admission 25.5M/113M per cell.
- Seeds: consumed 83001, 83003 (T=100) and 83002, 83004 (T=1000); data seed 2026083101; reserved 83011–83012 untouched.
- Status: diagnostic evidence against an exact posterior; not a benchmark or target claim.
- Outcome: 16/16 cells completed, zero-callback preflights, zero retained divergences/invalid/refinement-exhaustion stops. Cap/no-cap predictions held 16/16: I and D never cap at T=1000 (min bulk ESS ~6,000, kappa ≈ 13 independent of T); P mixes at depth 3–4 with min bulk ESS 17.8k–18.0k at T=1000 (ESS/call 0.14, 4.8× I, ~1,000× Q); Q caps 92–93% at T=1000 (step 0.0026, min bulk ESS 256–292, R-hat 1.025). Accuracy: mean z² 0.70–1.24, no |z|>5, variance ratios 0.94–1.06; one boundary accuracy-gate failure (T=100 D seed 83001, 1/100 coords at |z|=3.04) not replicated. The ±1 median-depth heuristic gate failed 9/16 because adapted steps sit ~3× below the stability limit; the Q "level direction is slow" mechanism clause was wrong (level is the stiff direction; fine-scale modes are slow).
- Diagnostics: full table in the study README and `artifacts/results-table.md`; reverse-coarser stops at level 0 in every arm (0.2–0.6% I/D/P, 5–8% Q) with no measurable bias on this target.
- Artifacts: `STUDIES/exact_state_space_ground_truth_v1/{PREREGISTRATION.md,protocol.json,README.md,analyze.py,src/main.rs,artifacts/…}`; `artifacts/summary.json` SHA-256 `a96e05928f63603bb72dbea4258d6de4532459867df986545f74e0aab5d6887f`; all hashes in `CHECKSUMS.sha256`; raw draws reproducible, not committed.
- Conclusion: the T=1000 phenomenology in the Polyscope ledger (83–92% caps, collapsed steps, ESS < 10) is reproduced in a controlled Gaussian by prior-based path metrics and eliminated by the posterior-precision tridiagonal metric, which the facade already represents. Not supported: any claim about the real target's global–path coupling or the globals' geometry.
- Next decision: freeze a fresh-seed canonical-v2 a=1 diagnostic with globals frozen (Kalman truth), arms I and P; then release (mu, log sigma_x) with a boundary-refreshed posterior-precision path block plus rank-2 arrowhead. No sspd-10 sampling authorised.

### WP6-FUNNEL-BIAS-FIX-V9 — root cause and correction of the funnel neck over-weighting
- Ordered time: 2026-08-31, after WP2-FUNNEL-REPRO-V1 and WP4-ESSGT-V1; kernel commit `29621b8`, study commit follows it.
- Protocol/config: `STUDIES/funnel_bias_fix_v1/protocol.json` SHA-256 `5079aba976d3343e7e1c0ed0a736252ab294804953b9d7b8f180ad5f5348dffb`; `PREREGISTRATION.md` `cf86a119e95e9eddf6c01da9936ca31fd4202cbd483bca908077d7bee5aad40a`; sampler revision `walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v9` (`src/kernel.rs` `23fd69c4…`, `src/walnutpie.rs` `22928d30…`); WP2 target/starts/tuning (10-D funnel, h=0.36, δ=0.21, 10 levels, depth 10, min micro 1, identity mass, 2,000 discarded, 4 chains, 1 thread); WP2 gates plus fix gates |ΔP(ω<−5)| ≤ 0.009, |ΔP(ω<−6)| ≤ 0.006, var(ω) ∈ [8.2, 9.8], zero retained divergences.
- Seeds: consumed 85001–85004 (F50), 85101–85104 (F), 85201–85204 (Gaussian G); Eight Schools regression reused WP3's seed 82001 with WP3's runner (both arms) as a paired `v8`/`v9` comparison, not fresh evidence for WP3's claim.
- Status: confirmed correction; root cause identified by differential oracle against the unmodified upstream implementation.
- Root cause: `kernel::integrate` decided micro-step acceptance on `max_j |H(z_j) − H(z_0)|` over every visited micro-step (documented as the "inclusive full-trajectory" tolerance) and the pinned macro-leaf oracle carved out the three upstream cases that disagreed as an intended "health correction". Upstream `walnutpie::macro_step`/`within_tolerance` decide on `|H(end) − H(start)|` only. The path-wide statistic is not symmetric under time reversal (reverse departures are measured from `H(z_ℓ)`), so the deterministic reverse selection could differ from the forward one while the kernel only re-checks *coarser* levels; non-reversible leaves were accepted and the funnel neck was over-weighted. A 4,000-leaf funnel oracle generated by the upstream headers (`oracle/walnutpie/f5bba365_funnel_leaves`, `funnel_leaf_cases.json` `7ed7ad47…`) disagreed with `v8` on 1,555 leaves (wrong acceptances, wrong rejections, different selected levels, different adaptation statistics) and agrees with `v9` on all 4,000 to 1e-11.
- Outcome: **all primary gates pass.** F50: P(ω<−5) 0.0474 vs 0.0478 (z −0.08), P(ω<−6) 0.0223 vs 0.0228 (z −0.14), var(ω) 9.04, q1% −7.01 (exact −6.98), mean ω +0.041 (MCSE 0.074), rank R-hat 1.0016/1.0010, bulk/tail ESS ω 1,644/2,134, 0 divergences / 0 invalid / 0 exhaustions / 43 depth caps in 200,000 transitions, 8,209,439 calls (v8: 12,770,697), 4.1 s. F (4×10k): z −0.29/−0.47, var 8.63, all gates pass. WP2's `v8` F50 at the same tuning was 0.0971/0.0557, var 11.41.
- Diagnostics: Gaussian G exact (mean z 0.16/−0.29, var 1.012/1.001, tail z 0.98/−0.50, zero health events). Eight Schools 82001 healthy under `v9` (max R-hat 1.0020/1.0026, min bulk ESS 1,907/1,233, zero divergences/depth caps); geomean bulk ESS/call `v9`/`v8` 0.84 (BPS) and 0.91 (multinomial) on one 4×1,000 seed — within ESS noise, reported not interpreted; BPS/multinomial remains 1.41×. All 142 crate tests, strict Clippy, fmt, and `-D warnings` rustdoc pass; no locked fingerprint changed because the frozen default tuning never reaches a leaf where the two statistics differ.
- Artifacts: `STUDIES/funnel_bias_fix_v1/{PREREGISTRATION.md,protocol.json,README.md,src/main.rs,src/eight_schools.rs,analyze.py,artifacts/…}`; `artifacts/F50.json` `b0d51a5e…`, `artifacts/summary.json` `67f923ec…`; full hashes in `CHECKSUMS.sha256`. Oracle: `oracle/walnutpie/f5bba365_funnel_leaves/{generate_funnel_leaves.cpp,funnel_leaf_cases.json,README.md,SHA256SUMS.txt}`; test `src/oracle_tests/funnel_leaf.rs`.
- Conclusion: the funnel bias was a kernel correctness defect in the acceptance statistic, not tuning; `v9` restores exact upstream leaf semantics and the paper's funnel marginal is reproduced at the paper's tuning. Claims not supported: that `v8` results on refinement-active targets (any `max_refinement_levels > 1` run that actually refined) are unbiased — every such prior study result is provisional until re-run under `v9`; that the WP2 arm-F ESS numbers are the kernel's efficiency (they were measured on a biased kernel).
- Next decision: ship `v9`; re-run refinement-active prior evidence (Neal funnel pilots v1–v3, Stock–Watson WP2b if it ran on `v8`, Eight Schools v38 timing) under `v9` before any external claim; WP1's paper adaptation may now be evaluated on the funnel.

### WP7-FUNNEL-ADAPTIVE-V1 — JMLR Appendix C warmup on the funnel (phase-2 arm A)
- Ordered time: 2026-08-31, after WP6-FUNNEL-BIAS-FIX-V9; kernel HEAD `1666dbb` (`v9`), paper adaptation `walnutpie-paper-adaptation-kquantile-gamma-v1`.
- Protocol/config: `STUDIES/paper_funnel_adaptive_v1/protocol.json` SHA-256 `1728d58bd6ea025e190985bda8a409826c1c3919957cba7b056327d9b54d7d92`; `PREREGISTRATION.md` `af0ab43d7014a586dcb9e8f659c3140fc69034fc341e3718aae61fb0cde09a5d`; 10-D Neal funnel, identity mass, 10 refinement levels, depth 10, min micro 1, 4 chains from ω ∈ {−3,−1,1,3}, 2,000 discarded (paper adaptation, mass adaptation off) + 4×50,000 retained, one thread, 1e9 callback cap, 1,200 s wall cap; conservative start δ = 1.0, h = 0.1; arms A2 (Δ = 2, p_a = .95, Γ = .8) and AD (Δ* = 0.72 = calibration-arm pooled q95 of the retained orbit energy range at the paper's fixed tuning, rule frozen before calibration); control F9 = `funnel_bias_fix_v1` F50 (not rerun). Gates as WP6 plus rank R-hat ≤ 1.01 and bulk/tail ESS ≥ 400 on ω and x₁.
- Seeds: consumed 87021–87024 (C, calibration), 87001–87004 (A2), 87011–87014 (AD), 87031–87034 (post-hoc CU, non-evidence). No confirmation seeds exist for this study.
- Status: preregistered diagnostic evidence; post-hoc CU labelled non-evidence.
- Outcome: **both adaptive arms pass every gate**; the preregistered default-recommendation rule is met only narrowly (A2 bulk ESS/call 0.82 × F9) and P2/P4 failed. A2: final δ 1.30–1.58, h 0.18–0.43 (spread 2.38×), P(ω<−5) 0.0423 (z −0.97), P(ω<−6) 0.0186 (z −1.05), var(ω) 8.80, R-hat 1.0031, bulk/tail ESS ω 1,501/1,448, 0/0/0 div/inval/exhaust, 206 depth caps, 8,841,164 retained calls, bulk ESS/call 0.82× and tail 0.61× F9. AD: δ 0.402–0.422 (1.05×), h 0.087–0.337 (3.86×), P(ω<−5) 0.0431 (z −0.78), P(ω<−6) 0.0209 (z −0.43), var 8.94, R-hat 1.0018, ESS 1,437/1,242, 0/0/0, 488 depth caps, 12,744,552 calls, 0.54×/0.36× F9. Retained q95 energy range: C 0.72, A2 1.82, AD 0.78 (both arms sit on the eq. 42 fixed-point invariant q95(range) ≈ Δ).
- Diagnostics: paper text states no numeric Δ (p_a = 0.95 as example, Γ = 0.8 "typically"); δ = 0.21/h0 = 0.36 are "auto-tuned" outputs. The δ rule converges within a few windows in both arms; its fixed point is a curve in (δ, h) (K95 = 3.4 at (0.21, 0.36) vs 1.9 at (0.41, 0.25), both = Δ*/δ), so the calibrated Δ* does not recover the paper's pair. The h rule tracks Γ = 0.8 inside every window but the unrefined fraction is position dependent on the funnel (post-hoc CU at fixed paper tuning: per-window 0.00–0.86, typically 0.4–0.75 — the paper's own tuning does not satisfy Γ = 0.8) and dual averaging restarts after every δ installation, giving per-window h swings of 10–30× and final h 0.045–0.96 across chains. Preflight zero callbacks; all cells under caps; A2 6.5 s, AD 10.2 s.
- Artifacts: `STUDIES/paper_funnel_adaptive_v1/{README.md,protocol.json,PREREGISTRATION.md,src/main.rs,analyze.py,CHECKSUMS.sha256,artifacts/*}`; `artifacts/A2.json` `7e319ba1417c94460c04a75d740bb6ac202dc5554d385f1babae0ee49a09e980`; `artifacts/AD.json` `de3543b31f2f15e4409f9059ab06aa258d24c3d1bc9024cfd32c5f2a92adab2a`; `artifacts/C.json` `fb6fb5bc14fd14bb0d47b07ef5bf7c0b4f9d9a37987ed147be77068081f9997d`; `artifacts/summary.json` `75133da1d63f73c82cab261e66d280a30abc2c52530949da984f4c7823034b21`; full list in `CHECKSUMS.sha256`.
- Conclusion: supported — the Appendix C mode is unbiased and health-clean on the funnel from a conservative start, and the K-quantile δ rule is reliable with Δ as its single knob. Not supported — that it reproduces the paper's (δ, h) pair, that Γ = 0.8 is the rule behind h0 = 0.36, or that it matches fixed paper tuning in efficiency (0.54–0.82× bulk, 0.36–0.61× tail ESS/call); the h rule is position/start sensitive on the funnel. No `src/` change was made (the instability is not an implementation defect relative to the paper's stated rule).
- Next decision: ship paper adaptation as documented opt-in, not as the default for hard targets. Preregister one generic h-rule stabilisation (no dual-averaging restart at δ installs, and/or pooled/cumulative unrefined statistic) as a separate study before any default change; fixed paper tuning (δ = 0.21, h = 0.36) remains the funnel reference configuration.

### WP8-EIGHT-SCHOOLS-V9-REBENCH-V1 — paired re-measurement of the public Eight Schools throughput claim on kernel v9
- Ordered time: 2026-08-31T06:48Z; kernel HEAD `1666dbb` (`walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v9`), after WP6-FUNNEL-BIAS-FIX-V9.
- Protocol/config: `STUDIES/eight_schools_v9_rebench_v1/protocol.json` SHA-256 `07756d5819fe5da09111aefbedc1b9154ef2f306673af1df44ed5ba953b009b6`; `PREREGISTRATION.md` `b4e7a6f0393faa5423ea85ac97cd2d0c9c0c83429a214bc50959cad0bb328404`; exact v38 strict track copied from `polyscope/STUDIES/public_owalnuts_numpyro_suite_v2/confirmation-v38/src/main.rs`: v38 noncentered Eight Schools density, four frozen starts, 4 sequential chains, 1,000 warmup + 1,000 retained, target .95, depth 8, `h=0.3`, 8 refinement levels, `max_error=1.0`, divergence threshold 1000, adapted diagonal from identity, one thread, 10M callback cap, timing strictly around `sample_chains_with_target_budget`; 5 timing repetitions per seed with bit-identity required; arviz 1.3 rank R-hat, bulk ESS, tail ESS (.05/.95); ESS/call uses total callbacks started as v38. Reference evidence: v38 `analysis-summary.json` SHA-256 `1fb8c295681a848280e126c815cb6e5f2d3a7c31682790d384af91d55c45e04f`; competitor minima from `RELEASES/owalnuts-x-2026-08-30-v3/finalized.json`.
- Seeds: paired 100070101–100070104 (the v38 evidence seeds, deliberately reused for a paired re-measurement of a frozen protocol — not fresh); fresh 88001–88003 (verified unused). No confirmation namespace.
- Status: paired re-measurement evidence for the throughput claim; not a new confirmation.
- Outcome: **claim survives v9 under both aggregations; published numbers were mislabelled.** True conservative minimum over all seven seeds and six functionals on v9: **12,830.11 bulk / 10,345.91 tail ESS/s** (2.04× CmdStan 6,290.30 bulk; 2.47× BlackJAX 4,195.13 tail); paired-4 release-style (min over functionals of seed-median) 15,373.43 / 11,530.33; fresh-3 true minimum 15,892.97 / 12,205.76. All seven v9 cells passed health (0 divergences, 0 depth caps, 0 invalid, 0 exhaustions, max rank R-hat 1.0029, min bulk ESS 1,763, min tail ESS 1,349) and every paired cell passed the v38 posterior-agreement rule against its v7 cell on all six functionals. Paired geometric-mean ESS per target call v9/v7: 0.961 bulk, 0.986 tail; total target calls within 1% per seed — the correction is cost-neutral on this target.
- Diagnostics: the release package's 19,054.65 / 14,494.34 is `summaries.rust.across_seeds.tau.{bulk,tail}_ess_per_total_second.median` in the v38 summary (min over functionals of the across-seed median), reproduced exactly by `analyze.py`; competitor figures in `validate_release.py` are true minima over all eligible cells. The like-for-like v7 minimum is 8,634.35 / 5,949.29 (seed 100070101, sampler wall 0.255 s vs 0.092–0.108 s on the other seeds), which still beat every strict competitor (1.37× / 1.42×). v9 walls 0.117–0.161 s (median of 5; ranges ≤ 0.008 s) were measured with three other agents running; wall-based v9 numbers are therefore conservative, and ESS/call is the machine-independent figure.
- Artifacts: `STUDIES/eight_schools_v9_rebench_v1/{protocol.json,PREREGISTRATION.md,README.md,RELEASE-NOTE.md,src/main.rs,analyze.py,artifacts/cell-*.json,artifacts/summary.json,artifacts/RESULTS.md,CHECKSUMS.sha256}`; `artifacts/summary.json` SHA-256 `4d8aa7aaae504797040097975ede57bd9126ee277822abf6a4a1ea31e0ce3765`; `src/main.rs` `fa52c079bb4c35b913b2fe22f5a1d399ebe5e9d64c80831236163f3624bd93d8`; `analyze.py` `8b69c706a8236a342ea02416f118002dd45451a095767946444b175aeaa77904`.
- Conclusion: supported — "fastest among strict matched competitors tested locally" on the v38 track holds on v9 with a ≥2× margin under the true conservative minimum; v9 does not change Eight Schools efficiency per gradient. Not supported — the specific published figures 19,054.65 / 14,494.34 as "conservative minima" (they are medians; the correct v7 minima were 8,634 / 5,949), and any wall-based v9 number as a clean-machine measurement.
- Next decision: publish the erratum drafted in `RELEASE-NOTE.md` with the v9 re-measured numbers; any future release package must compute the oWALNUTS aggregation with the same `minima()` rule as competitors. Re-measure on an idle machine before quoting v9 ESS/s externally.

### WP2b-SW-REPRO-V1 — paper Stock–Watson SV reproduction on simulated data
- Ordered time: 2026-08-31, sampling approximately 02:50–02:55 local (UTC−4);
  preflight (zero callbacks) preceded sampling; seven preregistration
  amendments were all recorded before any evidence was interpreted.
- Protocol/config: `STUDIES/paper_stock_watson_reproduction_v1/protocol.json`
  SHA-256 `fbb03f47384685f5a09af0c1aff0065730aec4df57e4ce28b70229965742cbc9`;
  `PREREGISTRATION.md` `7b73806c…edf9ef`; runner `src/main.rs` `505b0f72…26ad1`;
  kernel `walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v9` (`src/walnutpie.rs`
  `22928d30…54e2a40`, `src/kernel.rs` `23fd69c4…720eac` at checksum time).
  Target: JMLR §4.4 eqs. 35–38, shared σ, `σ⁻² ~ Gamma(5, .5)`, innovation
  parameterization, identity mass, dimension 756; simulated series (data seed
  2026083120 by preregistered range rule, `data.json` `df90ca84…a70b4`);
  starts on the simulated true paths with `φ` offsets ±0.2/±0.6 and 0.01
  jitter; 4 chains, 500 discarded / 2,000 retained, 4 threads; finite-penalty
  target policy emulating the reference's `−∞` failure semantics. Arms:
  F fixed paper tuning (h=.1, δ=.3, min 8 micro, 8 levels, depth 10);
  N NUTS-like control (h=.002, one level, δ=1000); A Appendix C adaptation
  from δ=1, h=.05 (Δ=2, p_a=.95, Γ=.8, no mass adaptation).
- Seeds: 84001 (F), 84002 (N), 84003 (A) consumed once for evidence; 84004
  consumed by non-evidence smoke arm S. Superseded start-rule/fixture runs
  reused 84001–84003 before any retained evidence existed (recorded in
  `PREREGISTRATION.md` amendments 1–7).
- Status: exploratory sampling evidence (preregistered; one seed per arm).
- Outcome: **paper claim not reproduced; adaptive arm passes.** F: all
  statistical gates pass (max R-hat 1.0016, min bulk 2,156, min tail 1,777)
  but 1 retained refinement exhaustion and 13.2% of orbits with
  `max H − min H > 2` (gate ≤ 1%). N: 1 divergence, 1 exhaustion, 99.99%
  depth-10 caps, only 0.10% of orbits above 2 (gate for the claim > 10%).
  A: all gates pass (max R-hat 1.0031, min bulk 1,363, min tail 2,088, zero
  health events), 2.0% of orbits above Δ = 2.
- Diagnostics: retained calls F 20,753,272 / N 8,191,457 / A 6,445,520; wall
  90.4 / 85.0 / 45.9 s; min bulk ESS per million calls 103.9 / 157.6 / 211.4;
  per second 23.9 / 15.2 / 29.7. F selected levels 4–5 (micro 7.8e-4–3.9e-4)
  on 86% of transitions with 44% reverse-coarser rejections; A landed
  δ = .567/.389/.362/.367 and h = .00371/.00424/.00515/.00419 (coarsest micro
  ≈ 5e-4) with window unrefined fractions .77–.81. Posterior means agree
  across arms within Monte-Carlo error.
- Artifacts: `artifacts/summary.json`
  `b0c9f88be9d551cf9ac939fab22a4551bdfaf92818c79d778d599ea1184abca3`;
  `F.json` `f77f1c51…adf1f6`, `N.json` `16a3b2bb…5feafe`, `A.json`
  `30c983f0…21abcd5`; all pinned in `CHECKSUMS.sha256`.
- Conclusion: on a simulated Stock–Watson series with paper-like latent
  ranges, the paper's fixed WALNUTS tuning is far too coarse (every macro
  step refines 4–5 halvings below h/8) and its orbit energy ranges exceed 2
  in 13% of orbits, while the fixed-step NUTS control is stable — so the
  Figure 16 contrast is data-specific and not supported here. The Appendix C
  adaptation implemented by WP1 finds a self-consistent δ/h, passes every
  gate, and is 2.0× more efficient per call than the paper tuning and 1.3×
  than the NUTS control. Not supported: any claim about the real series;
  cross-seed replication; comparison with an adapted external NUTS.
- Defects: (1) kernel parity — non-finite/recoverable evaluations stop the
  transition instead of failing the micro tolerance and halving as the
  reference does; the study had to emulate `−∞` semantics target-side.
  (2) Γ-rule counts transitions with zero built leaves as unrefined and drove
  h to the 1e6 bound under all-invalid transitions. (3) Per-transition
  Hamiltonian range for exhausted transitions is constant per chain.
- Next decision: fix defect (1) in the kernel (treat non-finite coarse
  attempts as failed tolerance), exclude zero-leaf transitions from the
  Γ statistic, then replicate arm A on fresh seeds and, if the real series
  can be obtained, on the paper's data.

### WP9-PAPER-H-RULE-STABILISATION-V2 — stabilising the Appendix C `h` rule and fixing its statistic
- Ordered time: 2026-08-31, after WP7-FUNNEL-ADAPTIVE-V1 and WP2b-SW-REPRO-V1; kernel HEAD `cfd813b` (`walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v9`) at sampling time; paper adaptation `walnutpie-paper-adaptation-kquantile-gamma-v2` at sampling time, `v3` after the default decision (commit `8cadd94`).
- Protocol/config: `STUDIES/paper_funnel_adaptive_v2/protocol.json` SHA-256 `713c96f9ba857b1d1290d6a0c8a15af7803e059ce1806a1c730b9cacb2e533f2`; `PREREGISTRATION.md` `a916f20f34dba5cc8c5f066a8f45c668d1af4c1532b6f329c82c6c89a1cb7c21`; 10-D Neal funnel, identity mass, 10 refinement levels, depth 10, min micro 1, 4 chains from ω ∈ {−3,−1,1,3}, 2,000 discarded (paper adaptation, mass adaptation off, start δ = 1.0, h = 0.1) + 4×50,000 retained, one thread, 1e9 callback cap, 900 s wall cap per arm; eight arms = Δ ∈ {2.0, 0.72} × {control: per-transition statistic + restart at δ installs; S: cumulative statistic; R: continue through δ installs; SR: both}. Gates: WP6/WP7 bias and health gates plus final-h max/min ≤ 1.5, final-δ max/min ≤ 1.2, bulk ESS(ω)/call ≥ 0.8× and tail ≥ 0.7× F9 (`funnel_bias_fix_v1` F50, `b0d51a5e…`).
- Seeds: consumed base seeds 89001, 89011, 89021, 89031, 89041, 89051, 89061, 89071 (chains base+0..3). No confirmation seeds exist for this study.
- Status: preregistered diagnostic evidence with a preregistered default decision rule.
- Outcome: **(b) continue-through-δ-install passes every gate in both families; cumulative statistic falsified.** A2-R: δ 1.116–1.254 (1.12×), h 0.511–0.618 (1.21×), P(ω<−5) 0.0503 (z +0.53), P(ω<−6) 0.0215 (z −0.39), var 8.97, R-hat 1.0012, bulk/tail ESS ω 1,383/2,057, 0/0/0 div/inval/exhaust, 31 depth caps, 4,739,088 retained calls, 1.41×/1.61× F9 bulk/tail ESS per call. AD-R: δ 0.353–0.402 (1.14×), h 0.365–0.465 (1.27×), P(ω<−5) 0.0504 (z +0.49), P(ω<−6) 0.0248 (z +0.57), var 9.15, R-hat 1.0012, ESS 1,575/1,666, 0/0/0, 68 caps, 6,994,024 calls, 1.09×/0.89× F9. Controls: A2-C h spread 1.68 (1.24×/1.18× F9, unbiased, healthy), AD-C h spread 2.77 (0.76×/0.54×). Cumulative arms: h spread 26.4× (A2-S, 4 retained refinement exhaustions), 44.8× (A2-SR), 95.2× (AD-S), 15.1× (AD-SR), 178–12,974 depth caps, 0.12–0.47× F9. All eight arms passed the bias gates. Preflight zero callbacks; walls 2.5–29.5 s.
- Diagnostics: predictions P1 7/8, P2 held, P3 held, P4 falsified, P5 falsified, P6 held for C/R only, P7 held for (b). Mechanism for the cumulative failure: dual averaging integrates `Γ − statistic`; a lagged running mean is a persistent offset that is integrated for hundreds of transitions before the mean catches up. Also fixed in this WP (revision `v2`, commit `cfd813b`): the `v1` unrefined fraction counted leaves rejected as invalid at the coarsest level as unrefined, so an all-invalid transition read as 1.0 and drove h to the 1e6 dual-averaging ceiling (reported by WP2b); the fraction is now taken over built leaves only (kernel `refinement_level_built` histogram), leaf-less transitions contribute no sample and no step update (`PaperAdaptationUpdate::transitions_without_statistic`), and the paper-mode step is bounded to `PAPER_STEP_RELATIVE_BOUND` = 1e3 × the configured initial step. The installed step was already the dual-averaging averaged iterate (no change). Acceptance-driven warmup and all fingerprints unchanged; 103 lib + 43 facade tests, strict Clippy, fmt, rustdoc clean.
- Artifacts: `STUDIES/paper_funnel_adaptive_v2/{README.md,PREREGISTRATION.md,protocol.json,src/main.rs,analyze.py,make_readme.py,CHECKSUMS.sha256,artifacts/*}`; `artifacts/summary.json` `ca0536eb7e05003dc8e0c85c7f66e686ba6415e0d743f5f9b0f017ca9f0455d1`; `artifacts/A2-R.json` `fdd708def3ed41a54e5db61cf508664bdfa54fb8e2e75a76703117f0d41a1453`; `artifacts/AD-R.json` `54c4c43bed0a1881779ddc621efaff427dc90a41bd7eb737bad08174bce5ba03`; source commits `cfd813b` (options + v2 statistic), `e16af20` (study), `8cadd94` (v3 default).
- Conclusion: supported — restarting dual averaging at δ installations is the cause of WP7's chain-specific h; one continued stream is stable (≤ 1.3×) and at Δ = 2 more efficient than the paper's fixed funnel tuning (1.41×/1.61× F9). Not supported — the cumulative statistic as a stabiliser (harmful), and any claim about targets other than the funnel; WP7's numbers are for revision `v1` and are unchanged.
- Next decision: `PaperRestartPolicy::ContinueThroughLocalErrorInstall` is the paper-mode default (`v3`, commit `8cadd94`); `RestartOnLocalErrorInstall` and `Cumulative` remain opt-in. Paper mode is release-ready as a documented opt-in with Δ as its single knob; re-run WP2b's paper-adaptation arm under `v3` before quoting Stock–Watson adaptive results.

### WP4B-REAL-TARGET-PATH-METRIC-V1 — canonical-v2 at T=1000: NumPyro reference, centered adapted diagonal, and the posterior-precision path block
- Ordered time: preregistered 2026-08-31T05:5xZ; oWALNUTS arms executed ~06:2x–06:3xZ; NumPyro arm N ~06:25–07:05Z; post-hoc arms ~06:45–06:55Z; finalized 2026-08-31T07:11Z (UTC).
- Protocol/config: `STUDIES/real_target_path_metric_v1/protocol.json` SHA-256 `9ac8722cec8941f0260a262193f352cfdeb30ddf0917e358f3e16cf9f31ca9cb`; target `polyscope-canonical-v2` ported verbatim (oracle parity 4/4 in Rust and JAX); fixtures sspd-11 (`2fff9766…83baad`, primary), sspd-10 (`005a5c7d…687ac`, pathological), sspd-05 (`1d10f68e…8091d`, T=100 sanity); kernel `walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v9` at `1666dbb` (post-hoc arms at `0a04c2d`, `src/` identical); oWALNUTS arms: depth 8, refinement 3, max error 1.0, dual-averaged step at 0.8 with initial step search, 4 chains, 500/2,000; arm N: NumPyro 0.21 NUTS, depth 12, 1,000/2,000, target 0.9, adapted diagonal, a ∈ {1, 0.75}; shared data-informed starts with `mu` offsets (sspd-11 cold factor deliberately not applied); admission 25.5M/113M per T=1000 arm, zero callbacks in every preflight.
- Seeds: consumed 86001 (sspd-11), 86002 (sspd-10), 86003 (sspd-05) for every arm; no reserved seeds.
- Status: preregistered diagnostic evidence with external reference; post-hoc arms P2/FI/FP are labelled exploratory.
- Outcome (gates: R-hat ≤ 1.01, bulk/tail ESS ≥ 400 on eight functionals, zero retained divergences/invalid/exhaustion, cap rate ≤ 1%, P-vs-N means within 3 combined MCSE):
  sspd-11 — N(a=1) PASS (max R-hat 1.0073, min bulk 853, median depth 6); N(a=.75) FAIL narrowly (1.0124, 573); I PASS (1.0053, 955, cap 0.79%, median depth 8; 2.16M calls); P PASS (1.0044, 795, cap 0.04%, depth 6; 598k calls; 2.8× I's ESS/call; agreement z ≤ 1.25); B FAIL narrowly (1.0103, 445, cap 0.08%, depth 6).
  sspd-10 — every arm FAILS including the reference: N(a=1) R-hat 1.2944, min bulk 11.5, 1,510 divergences, 13.0% depth-12 hits, 648 s; N(a=.75) 1.5318, 7.2, 913 divergences; I/P/B caps 74%/33%/50%, steps 6e-4–1.3e-3, min bulk 4–5, R-hat 2.8–3.9.
  sspd-05 — I PASS (1.0057, 523); P FAIL (1.0250, 273, tail 129); B FAIL (1.0129, 340); N(a=1) FAIL (1.06, 56, tail 19); N(a=.75) FAIL narrowly (1.0134, 524).
  Post-hoc, globals frozen at arm I posterior means: FP (posterior-precision block) vs FI (identity) — sspd-11 depth 4 vs 5, 163k vs 477k calls, 1.3e-1 vs 2.5e-3 bulk ESS/call (≈50×); sspd-10 depth 4 with 0 caps vs depth 8 with 74% caps, 1.0e-1 vs 5.0e-4 ESS/call (≈200×); sspd-05 ≈6×. Post-hoc P2 (accurate path block, globals free) was worse than P everywhere (sspd-11 depth 7, 2× calls, min bulk 549; sspd-10 66% caps).
- Diagnostics: predictions — (1) P passes sspd-11: held for the gates, failed the depth ≤ 5 clause (median 6); (2) I passes, no cap: held; (3) B caps ≫ 10%: **falsified** (0.08%); (4) sspd-10 reported: all arms and the reference fail; (5) sspd-05 all pass: only I (and no N run) passed. Full tables in `STUDIES/real_target_path_metric_v1/README.md` and `artifacts/owalnuts-v1/results-table.md`.
- Artifacts: `STUDIES/real_target_path_metric_v1/{PREREGISTRATION.md,protocol.json,README.md,src/,make_starts.py,numpyro_reference.py,analyze.py,checksums.py,starts/,fixtures/,artifacts/numpyro/,artifacts/owalnuts-v1/,artifacts/owalnuts-v1-posthoc/}`; `artifacts/owalnuts-v1/summary.json` SHA-256 `4a25f7cb0bc99dffb27289d63693d89eb7b1f2982fc561cc90131eb71c235341`; raw functional draws hashed in `CHECKSUMS.sha256`, not committed.
- Conclusion: (a) the first external baseline on the T=1000 fixtures shows sspd-10 is not sampled by NumPyro NUTS at depth 12 either (hundreds to thousands of divergences): Polyscope rows 72/75/77 compared arms on a target no tested Euclidean sampler handles and are uninformative about the sampler; (b) on the non-pathological T=1000 cell, centered adapted-diagonal oWALNUTS passes at depth 8 and the a=0.75 production baseline only narrowly fails — "any diagonal metric caps at T=1000" is false as a general claim; (c) the posterior-precision tridiagonal path block is the correct conditional metric on the real target (≈50–200× ESS/call with globals frozen, zero caps on the funnel cell) and gives 2.8× ESS/call with globals free on sspd-11; (d) with globals free a stiff correct path block hurts (P2 < P) because the innovations are in absolute units in every centeredness, so all parameterizations share the `sigma_x → 0` funnel — the residual problem is global–path scale coupling, not the path block. Not supported: any oWALNUTS-vs-NUTS efficiency claim (work units differ), any claim about the sspd-10 posterior.
- Next decision: (1) product: sample canonical-v2 in a=1 coordinates with the posterior-precision path block refreshed from adapted globals at slow-window boundaries (stage-6 machinery + linear-time `BidiagonalCholesky`); (2) model: version canonical-v3 with scale-non-centered innovations `epsilon_t = (q_t − mu)/sigma_x` before any further funnel-cell work — no Euclidean metric fixes a `sigma_x → 0` funnel; (3) retire sspd-10 as a qualification fixture; keep it as a stress cell with the NumPyro failure as its reference; (4) freeze a fresh-seed confirmation of arm P on sspd-11 with three seeds before any product claim.

### WP10-INVALID-EVALUATION-PARITY-V10 — recoverable failures refine like upstream
- Ordered time: 2026-08-31, after WP9 and WP4b; kernel commit `452befb`
  (`walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v10`), study commit follows.
- Protocol/config: `STUDIES/invalid_evaluation_parity_v1/PREREGISTRATION.md`
  SHA-256 `1f028fe997a5…` (two amendments, both recorded before the affected
  artifact was interpreted); sub-protocols `truncated/protocol.json`
  `7cd68f903fa5…`, `funnel/protocol.json` `1266e971040b…` (frozen copy of WP6's),
  `stock_watson/protocol.json` `09453694c122…` (frozen copy of WP2b's with the
  finite-penalty emulation removed: `nonfinite_policy = recoverable`);
  `src/kernel.rs` `112eb2658099…`, `src/walnutpie.rs` `58d880c2f019…`. Three
  arms: truncated 2-D Gaussian (h=0.9, δ=0.5, 6 levels, depth 6,
  4×500/50,000), Stock–Watson F (paper tuning) and A (Appendix C, paper-mode
  v3 defaults) at 4×500/2,000 on WP2b's series, funnel F at paper tuning
  4×2,000/20,000. Full hashes in `CHECKSUMS.sha256`.
- Seeds: consumed 90031–90034 (truncated), 90011–90014 (SW F), 90021–90024
  (SW A), 90001–90004 (funnel); post-hoc non-evidence replicate 90041–90044.
- Status: confirmed correction (leaf level) plus exploratory sampling
  evidence (sampler level, one seed set per arm).
- Root cause: through `v9` the facade mapped `TargetError::recoverable` to a
  NaN evaluation and the kernel stopped the whole transition
  (`StopReason::InvalidEvaluation`, counted as a divergence). Upstream
  `walnutpie::detail::NoExceptLogpGrad` (`util.hpp`) maps a failed evaluation
  to `logp = -inf`, `grad = 0`; `macro_step`/`within_tolerance` then see an
  infinite endpoint error, refine, and only `build_leaf` returning `nullopt`
  after every halving stops the orbit in that direction. `reversible` treats
  a `-inf` endpoint identically (never within tolerance). The kernel now
  implements exactly this: `-inf` with a finite gradient is a zero-density
  point that integration continues through (with the supplied zero
  gradient), excluded from the Hamiltonian extrema and divergence statistic,
  reported as `zero_density_evaluations`; NaN/`+inf`/nonfinite gradients stay
  malformed and fatal. Detailed balance: the leapfrog map with a
  position-dependent (zero on the region) force is still a reversible
  volume-preserving involution and acceptance depends only on the two
  endpoint Hamiltonians, so the target restricted to its support is invariant;
  forward and reverse selections agree because both use the same endpoint
  statistic.
- Outcome: **leaf oracle passes 4,000/4,000.** New differential oracle
  `oracle/walnutpie/f5bba365_invalid_leaves` (`invalid_leaf_cases.json`
  `7dc3f587d3e0…`), generated by a C++ driver against the unmodified upstream
  headers with a throwing wall target wrapped in `NoExceptLogpGrad` (343
  wall-touching leaves, 50 accepted after refining away): acceptance, calls,
  zero-density call count, adaptation statistic and endpoint agree to 1e-11;
  under `v9` every wall-touching leaf stopped on its first wall call.
  Sampler level: truncated target — all gates pass, every draw inside,
  5,061,666 recoverable = zero-density evaluations, 0 invalid stops, 0
  divergences, x₀ mean 0.8046 (exact 0.7979, z +2.41), var 0.3684 (0.3634,
  z +2.23), x₁ mean z −0.80, var z +1.62; post-hoc replicate on seed 90041
  gave x₀ z −0.12 / −0.11, so the offset is sampling variation.
  Stock–Watson F: 3,404,887 recoverable evaluations (16.5% of 20.57M retained
  calls) with **0 invalid-evaluation stops**, max gated R-hat 1.0011, min
  bulk/tail ESS 2,268/2,453, 1 exhaustion, 13.0% orbits with H range > 2,
  110 bulk ESS per M calls (WP2b emulated: 104); A: 522 recoverable, 0
  stops, max gated R-hat **1.0101 (fails ≤1.01 on log σ²)**, min bulk/tail
  630/1,051, 0/0/0 health, 6.8% > 2, 219 ESS per M calls (WP2b 211).
  Funnel: P(ω<−5) 0.0464 (z −0.17), P(ω<−6) 0.0206 (z −0.38), var ω 8.65,
  0/0/0/7 health, all gates pass.
- Diagnostics: 146 → 154 crate tests (110 lib + 44 facade), strict Clippy,
  `fmt --check`, `-D warnings` rustdoc pass; no locked fingerprint changed
  (no frozen fixture returns a recoverable error); facade tests
  `recoverable_target_failures_are_deterministic_rejections_with_exact_partitions`
  (rewritten for v10 semantics) and
  `zero_density_boundary_keeps_truncated_target_stationary` (new).
- Artifacts: `STUDIES/invalid_evaluation_parity_v1/{README.md,RESULTS.md,
  PREREGISTRATION.md,summarize.py,CHECKSUMS.sha256,artifacts/summary.json
  92080b223ea5…, truncated/artifacts/{T.json a9aea6ea23d0…, T2.json
  9e46678b187c…}, funnel/artifacts/F20.json f68196c70f06…,
  stock_watson/artifacts/{F.json 8f6c03ee4cbd…, A.json b95e7f070714…}}`;
  oracle `oracle/walnutpie/f5bba365_invalid_leaves/{generate_invalid_leaves.cpp,
  invalid_leaf_cases.json,README.md,SHA256SUMS.txt}`; test
  `src/oracle_tests/invalid_leaf.rs`.
- Conclusion: WP2b defect (1) is fixed with reference semantics; stiff
  models whose coarse micro-steps overshoot into a non-representable region
  no longer produce no-op transitions. Claims not supported: cross-seed
  replication of the Stock–Watson arms; arm A meeting the R-hat gate (it
  missed by 0.0001 on one seed under paper-mode v3 defaults, which WP2b did
  not use); any efficiency claim beyond "ESS/call within ±10% of WP2b".
- Next decision: ship `v10`; re-run the WP2b arm A on fresh seeds under v3
  defaults before quoting it; the `StopReason::InvalidEvaluation` variant is
  now unreachable for successful runs and may be removed at the next
  breaking release.

### WP12-SSPD11-CONFIRMATION-V1 — fresh-seed confirmation of arms I and P on sspd-11 (kernel v10) and Stock–Watson arm A robustness
- Ordered time: 2026-08-31, preregistered before sampling (protocol hashes below), sampling ~03:22–03:27 local (UTC−4), after WP10 (`93ed8e9`).
- Protocol/config: `STUDIES/sspd11_confirmation_v1/PREREGISTRATION.md` SHA-256 `cd3beed410510b5d…`; `primary/protocol.json` `2679b4edc6bf582c…` (WP4b's protocol with fixture sspd-11 only, arms I and P only, seeds as a list); `stock_watson/protocol.json` `8b4c5010b72948af…` (WP10's Stock–Watson sub-protocol with three copies of arm A at fresh base seeds); runners copied from WP4b (`primary/src/main.rs`, seed loop added) and WP10 (`stock_watson/src/main.rs`, verbatim). Kernel `walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v10` at `93ed8e9` (`src/kernel.rs` `112eb2658099…`, `src/walnutpie.rs` `58d880c2f019…`). Primary: fixture sspd-11 (`2fff9766…83baad`), a=1, 4 chains, 500 / 2,000, depth 8, refinement 3, max error 1.0, dual-averaged step at 0.8 with initial step search; I = identity initial diagonal with mass adaptation; P = `StructuredBlockMass([globals diag from arm I of the same seed, BidiagonalCholesky path block at data-informed globals])`, mass adaptation off. Gates exactly WP4b's (R-hat ≤ 1.01, bulk/tail ESS ≥ 400 on eight functionals, zero div/invalid/exhaustion, cap ≤ 1%) plus P-vs-I (same seed) and P-vs-N agreement within 3 combined MCSE, N = WP4b NumPyro a=1 seed 86001 (`sspd-11-N-a1-86001.json` `e8587b3d…63cd6`). Confirmation rule: all three seeds pass every gate. Secondary: WP10 arm A configuration unchanged (Appendix C, paper-mode v3 defaults, natural recoverable errors, 4×500/2,000). Full hashes in `CHECKSUMS.sha256`.
- Seeds: consumed 91001–91003 (primary, both arms) and 91011–91013 (Stock–Watson A1–A3); verified unused beforehand; no reserved seeds.
- Status: confirmed sampling evidence for arm I; negative confirmation (not confirmed) for arm P; exploratory robustness evidence for Stock–Watson arm A.
- Outcome: every preflight started zero callbacks; no wall cap hit; no deviation. Arm I passed 3/3 (max R-hat 1.0066 / 1.0072 / 1.0035, min bulk 931 / 962 / 919, min tail 2,119 / 1,884 / 1,714, 0/0/0 health, cap 0.40% / 0.61% / 0.39%, median depth 8, max |z| vs N 1.83 / 0.89 / 1.46) → **confirmed**. Arm P passed 2/3: seeds 91002 and 91003 pass every gate (max R-hat 1.0070 / 1.0058, min bulk 697 / 740, min tail 962 / 776, cap 0.04% / 0.05%, depth 6); seed 91001 fails only the R-hat gate on `beta` (1.0102 vs 1.01; other seven functionals ≤ 1.0040; bulk 691 / tail 624; 0/0/0 health; cap 0.01%; agreement |z| 1.86 vs I, 1.25 vs N) → **not confirmed** under the conjunctive rule. Efficiency P/I (min bulk ESS per retained call): 2.573, 2.963, 2.683; geometric mean 2.735; wall P 7–9 s vs I 24–28 s. Stock–Watson A1/A2/A3: gated max R-hat 1.0074 / 1.0071 / 1.0101 (`log_sigma2`), min bulk 592 / 463 / 484, min tail 1,287 / 796 / 527, 0/0/0 health, zero caps, δ landing 0.24–0.37, h 0.0060–0.0070 → pass rate 2/3.
- Diagnostics: predictions — P confirmed 3/3 **failed**; I confirmed 3/3 held; P/I ≥ 2 held (2.74); agreement on every seed held (max |z| 1.92); depth (P 6, I 8 with cap ≤ 1%) held. Stock–Watson predictions held (2/3 or 3/3 pass; max R-hat within [1.000, 1.015]; clean health). Tables: `STUDIES/sspd11_confirmation_v1/README.md`, `artifacts/RESULTS.md`.
- Artifacts: `STUDIES/sspd11_confirmation_v1/{PREREGISTRATION.md, README.md, analyze.py, checksums.py, CHECKSUMS.sha256, primary/{protocol.json, src/, fixtures/, starts/, artifacts/primary-v1/*.json, artifacts/primary-v1-log.txt}, stock_watson/{protocol.json, src/, artifacts/{preflight.json, data.json, A1-3.json, A1-3.log}}, artifacts/{summary.json, RESULTS.md}}`; raw functional draws (`primary/artifacts/primary-v1/draws/*.f64`) hashed in `CHECKSUMS.sha256`, not committed.
- Conclusion: supported — on the non-pathological T=1000 canonical-v2 fixture in a=1 coordinates, oWALNUTS with a plain adapted diagonal passes modern gates at depth 8 on three fresh seeds and agrees with NumPyro NUTS (product-facing claim for the centered-coordinate path); the posterior-precision path block is healthy, unbiased (agrees with I and N on every seed) and ≈2.7× more efficient per target call with a stable range. Not supported — a claim that arm P passes the strict 1.01 R-hat gate on every seed at 500 / 2,000 (one single-functional miss of 0.0002), and any Stock–Watson arm A pass claim beyond 2/3 at 4×2,000 (both misses are hairline R-hat excursions with clean health and ESS above the gates, consistent with run length rather than a seed-specific pathology).
- Next decision: freeze a P confirmation at 4×4,000 retained draws (or with the path block refreshed at slow-window boundaries per WP4b) as a new preregistration; do not relax the gate. Arm I may be cited now as confirmed. Stock–Watson arm A: report as 2/3 at this draw count; a 4×4,000 rerun would resolve the run-length question if it matters for the release notes.

### WP15a-AUTODIFF-BRIDGESTAN-ENZYME-V1 — autodiff track: survey, BridgeStan integration, Enzyme feasibility
- Ordered time: 2026-08-31, after WP12/WP13; exact UTC not recorded.
- Protocol/config: engineering evidence, not preregistered. Kernel v10 at `d8617a8`+; GNU Rust 1.88; BridgeStan 2.9.0 (mingw g++ 16.1.0, STAN_THREADS=true); `reverse` 0.2.2 tape; benchmarks in `integrations/bridgestan/src/bin/bench.rs` and `integrations/enzyme/src/bin/bench.rs`.
- Seeds: reused v38-ablation seeds 82001–82003 for the paired Eight Schools re-measurement (paired by design); 84101/84201 for local-level engineering runs. No preregistered namespace consumed.
- Status: feasibility/engineering evidence.
- Outcome: BridgeStan `Target` works end to end. Agreement at 20 random points: Eight Schools |Δlp| ≤ 1.4e-14, |Δgrad| ≤ 2.9e-16; local level gradients ≤ 4.5e-13 (value differs by Stan's normalizing constants only). Per-call gradient: 6.7 µs vs 40 ns hand (Eight Schools, 167×), 8.4 µs vs 201 ns (T=100, 42×), 38 µs vs 2.9 µs (T=1000, 13×). Paired sampling (identical seeds/settings): Eight Schools trajectories bit-identical, ESS/s 12.5k→1.2k (v38 config) and 38.6k→3.4k (paper adaptation); local level ESS/s ratio 4.7×/3.7× (T=100, 1/4 threads) and 2.3×/2.5× (T=1000); STAN_THREADS 4-thread speed-up 2.6–3.3× on one shared instance; 0 recoverable failures in 1.16M calls; Stan domain exceptions map to recoverable/zero-density (tested). Enzyme: nightly `rustc 1.100.0-nightly (2026-08-30)` accepts `-Zautodiff` but the sysroot ships no `libEnzyme-23` on Windows; not usable (probe committed). Tape fallback (`reverse` 0.2.2): gradients agree to 1e-13 but 58–68× hand per call, ESS/s 4.3× (T=100) to 11× (T=1000) worse — off-the-shelf Rust tapes are not competitive.
- Diagnostics: `integrations/bridgestan/artifacts/bridgestan-benchmark.json`, `integrations/enzyme/artifacts/tape-benchmark.json`; tests 4 (bridgestan, incl. concurrency + exception mapping) + 2 (tape parity); strict Clippy/fmt clean on both crates.
- Artifacts: `integrations/AUTODIFF-RESEARCH.md` (survey, measurements, facade proposal), `integrations/bridgestan/`, `integrations/enzyme/`; model shared objects are build artifacts and untracked.
- Conclusion: supported — BridgeStan is the right first autodiff integration; oWALNUTS can sample any Stan model today with Stan-level gradient cost, and the T=1000 state-space case keeps a 2.3–2.5× ESS/s penalty only. Not supported — any claim that Eight Schools ESS/s throughput survives autodiff gradients (the 6.7 µs Stan gradient dominates a 40 ns hand gradient; the public throughput claim must say "hand-written target"); any near-term Rust-native (Enzyme/tape) route.
- Next decision: preregister a same-`.stan`-file three-way (oWALNUTS-BridgeStan vs CmdStan vs nutpie); land the small additive facade items (`?Sized` targets, error-mapping contract, `parameter_names`); re-probe Enzyme when a rustup component ships.

### WP14-NUMPYRO-COMPARISONS-V10-V1 — sspd-05 timing, real-market T=48, and the funnel, measured against NumPyro on kernel v10
- Ordered time: 2026-08-31 (after WP12/WP13; kernel v10 at `d8617a8`).
- Protocol/config: `STUDIES/numpyro_comparisons_v10_v1/{PREREGISTRATION.md,protocol.json}`, frozen before sampling; three parts — (1) exact `matched-timing-v1` replication on v10 plus adapted-diagonal and path-block arms (sspd-05, a=1, 500/4,000, accept .9, depth 8, one thread, frozen shared diagonal `afbc3318…d63ea`); (2) real-market T=48 (`73185a16…714a`, wrapping Phase 0 fixture `cf929ec6…8aa6`) at depth 10, a=0.75 and a=1, vs Phase 0.2 NumPyro settings; (3) 10-D Neal funnel, 2,000 + 4×50,000, oWALNUTS fixed paper tuning and Appendix C v3 vs NumPyro NUTS accept .8/.95 depth 10. ArviZ 1.3 diagnostics from exported draws, one code path per backend; JAX transcription parity-checked against the pinned oracle (4/4).
- Seeds: evidence 92001–92003 consumed once per backend cell; compile probe 92000 non-evidence; verified fresh before freezing.
- Status: exploratory sampling evidence (paired, preregistered, fresh seeds).
- Outcome: part 1 — primary T-F/T-N bulk ESS per sampling second **4.03** (tail 4.12; 2/3 eligible; per-work-unit ratio 1.00, labelled); T-N 3/3, T-F 2/3 (seed 92002 `alpha` R-hat 1.0594 under the nine-functional gate), T-I 1/3 (exhaustion gate), T-P 0/3; P1 held, P2/P3 failed — **the v7 figure 5.4–7.3× is retired; the path block has no advantage at T=100** (per-call ratios 2.26/0.51/0.52). Part 2 — R-B, R-I, R-N all **3/3**; zero depth caps at depth 10 (pre-v9 pilot: 3.625%); ESS/s R-B/R-N 3.82, R-I/R-N 3.17; per work unit 0.48/0.41 (labelled); P4/P5/P6 held — **the last NumPyro-favourable comparison is retracted**. Part 3 — NumPyro under-covers the neck on 6/6 cells (P(ω<−5)=0.0000 in five, 0.0115 in one; exact 0.0478), var ω 5.34–8.80, divergences 365–3,449 per cell, 0/6 gates; oWALNUTS FN-F **3/3 exact** (P(ω<−5) .0461–.0510, 0 divergences), FN-A 2/3 (one ω R-hat 1.0102 miss, tail masses exact); P8/P9/P10 held, P7 failed.
- Diagnostics: full per-cell tables in `artifacts/RESULTS.md` and `artifacts/summary.json`; machine shared with two concurrent agents (wall caveat recorded; ESS/work is the robust figure).
- Artifacts: `STUDIES/numpyro_comparisons_v10_v1/` (protocol, sources, per-cell JSON, RESULTS.md, summary.json); raw draws hashed in `CHECKSUMS.sha256`, not committed.
- Conclusion: supported — ~4× wall advantage on the matched sspd-05 track at per-work-unit parity; real-market T=48 passes 3/3 at depth 10 in both parameterizations, 3.2–3.8× ESS/s; NumPyro NUTS catastrophically under-covers the funnel neck where oWALNUTS v10 is exact. Not supported — the v7 5.4–7.3× magnitude; any small-T path-block advantage; frozen-metric robustness on all nine sspd-05 functionals; FN-A/paper-mode borderline ω R-hat at 4×50k (recurrent, third sighting).
- Next decision: update the public claims table (README/release page) with the corrected 4× figure and the funnel measurement; version a paper-mode R-hat robustness look (longer runs or pooled-δ) before quoting FN-A 3/3 anywhere; no confirmation seeds consumed.

### WP15B-PYTHON-TARGETS-V1 — Python package with callable/JAX/PyTorch/PyMC targets and overhead benchmark
- Ordered time: 2026-08-31, after WP15A survey sections existed; exact UTC not recorded.
- Protocol/config: `integrations/python/BENCH.md` (frozen before execution); kernel v10 at `d8617a8`; extension `owalnuts 0.1.0b2` (PyO3 0.28, GNU 1.88); Python 3.11.16; Eight Schools v38 settings (1,000/1,000, accept .95, depth 8, 8 levels, budgeted entry point) and WP4 local-level settings (500/2,000 or 1,000, accept .8, depth 8, 3 levels, mass adaptation off) with identity and tridiagonal posterior-precision metrics.
- Seeds: 93001–93003 consumed (verified fresh before use); data seed 2026083131+T.
- Status: engineering evidence with bounded exploratory benchmarks; not a public benchmark claim (shared machine; NumPyro/nutpie work counters are leapfrog proxies, labelled).
- Outcome: package built and 10/10 adapter tests pass, including v10 zero-density refine-through from Python (`-inf`/exception → recoverable; truncated-Gaussian moments exact; zero invalid stops). Overhead per fused call: native 0.6–0.8 µs, PyMC 6 µs, numpy 10–13 µs, JAX 26–84 µs, torch 170–290 µs. Geomean min-bulk ESS/s: Eight Schools — owalnuts+pymc 1,439 vs NumPyro warm 897 (native 15,226; 44,749 at 4 threads); T=1000 local level — numpy+precision metric 2,455 vs NumPyro identity 720. owalnuts loses on pure-JAX targets (414 vs 897) and against nutpie on PyMC models (1,298–2,120 vs 17,844–25,043 ESS/s; nutpie is GIL-free numba cfunc on 4 cores; per-gradient efficiency comparable). GIL serialises Python targets: threads=4 is slower than threads=1 for numpy.
- Diagnostics: zero divergences, zero max-depth caps, zero invalid-evaluation stops in every oWALNUTS cell; worst R-hat 1.0061; posterior agreement max |z| 2.44 across backends and references.
- Artifacts: `integrations/python/` (crate, package, tests, BENCH.md, bench/run_bench.py, bench/pymc_compare.py, bench/artifacts/summary.json, bench/artifacts/pymc-compare.json, bench/artifacts-full.log); design and proposals in `integrations/AUTODIFF-RESEARCH.md` §"Python callable targets (WP15b)".
- Conclusion: supported — oWALNUTS from Python beats NumPyro when the gradient is compiled outside JAX and whenever a structured metric applies; not supported — any claim on pure-JAX targets or vs nutpie's GIL-free transport. The nutpie gap is callback transport, not sampler efficiency.
- Next decision: implement a GIL-free raw-pointer target entry in the facade (proposal 2), then rerun the PyMC comparison; propagate target error messages through `Error` (proposal 1).

### WP16-REFRESHED-PATH-BLOCK-V1 — boundary-refreshed structured metrics: facade driver, P confirmation, refresh policy falsified
- Ordered time: 2026-08-31, preregistered before sampling; runs after facade commit `b413c88`.
- Protocol/config: `STUDIES/sspd11_refreshed_block_v1/{PREREGISTRATION.md, protocol.json}` (WP12's protocol extended: arms I/P/R, retained ∈ {2,000, 4,000}, fresh seeds; gates unchanged incl. agreement vs arm I and the WP4b NumPyro a=1 reference `e8587b3d…63cd6`). Facade addition under test: `sample_chains_structured_refresh` (`walnutpie-structured-metric-refresh-v1`, kernel `ALGORITHM_REVISION` v10 unchanged; identity refresh proven bit-identical to the fixed direct driver; 159 tests, strict Clippy/fmt/rustdoc clean). Hashes in `CHECKSUMS.sha256`.
- Seeds: consumed 94001–94003 (sspd-11, all arms, both draw counts) and 94001 (sspd-05 sanity, arms I/R); smoke seed 9999 labelled non-evidence. No reserved seeds.
- Status: confirmed sampling evidence for arm P at 4×500/4,000; confirmed for arm I at both counts; negative (policy falsified) for arm R; engineering evidence for the driver itself.
- Outcome: every preflight zero-callback; no wall caps; 20/20 cells completed. **Arm P confirmed 3/3 at 4×500/4,000** (worst R-hat 1.0030–1.0070, min bulk 1,142, zero div/invalid/exhaustion, caps ≤0.03%, agreement max |z| 2.33; ESS/retained-call 2.2–2.3× arm I) — closing WP12's open confirmation (its 2,000-draw miss was run length, prediction P5 held). **Arm R (per-window refresh) not confirmed**: 2/3 at both counts; seed 94001 cap rate 1.38%/1.26% and worst R-hat 1.0198 at 2,000; ESS/call only 0.25–0.52× P and 0.58–1.11× I (predictions P2–P4 failed; P1 held — 16/16 installs, zero refresh failures).
- Diagnostics: R's failure mechanism is estimation, not machinery — early slow windows sampled at small steps underestimate a slow global's variance; seed 94001 chain 2 installed globals precision entries up to ≈2,000 (windows 1–3), boundary step searches oscillated (0.086 → 0.0069), and a ≈5% depth-8 tail persisted after freeze. Full per-boundary telemetry in the run artifacts.
- Artifacts: `STUDIES/sspd11_refreshed_block_v1/{PREREGISTRATION.md, protocol.json, README.md, src/, analyze.py, checksums.py, CHECKSUMS.sha256, artifacts/{run-2000,run-4000,sanity,smoke}/*.json, artifacts/{summary.json, RESULTS.md}}`; raw draws hashed, not committed.
- Conclusion: supported — the refresh driver is correct and exact (installation seam bit-identity, zero-callback preflight, deterministic parallel), and the one-shot posterior-precision path block is a confirmed 2.2× efficiency win over adapted-diagonal at T=1000 on fresh seeds at 4×500/4,000. Not supported — per-window refresh as a policy; any claim that arm P passes at 4×500/2,000 (2/3 there).
- Next decision: keep the driver opt-in; do not default per-window refresh. Candidate next preregistration: final-boundary-only rebuild from the longest window (self-contained R without arm-I input), or arm P with a calibrated globals diagonal, as the production path.

### WP18-GIL-FREE-TRANSPORT-V1 — RawTarget facade surface and the nutpie rebench
- Ordered time: 2026-08-31, after WP16/WP17 study commits; exact UTC not recorded.
- Protocol/config: `integrations/python/BENCH.md` §"GIL-free rebench (preregistered before execution; WP18)", frozen before running; facade commit `3b14d64` (`RawTarget`/`RawTargetFn`, `&dyn`/`Box`/`Arc` target impls, fatal target messages in `Error::Display`, `Target::parameter_names`; default behaviour, fingerprints and oracles unchanged; kernel `ALGORITHM_REVISION` v10). Package: `owalnuts 0.1.0b2` + `from_cfunc`/`from_pymc(gil_free=True)`; Eight Schools PyMC model, 1,000/1,000, 4 chains, accept .95, step 0.3, depth 8, 8 levels; local-level T=1000, 500/2,000, accept .8, depth 8, 3 levels, tridiagonal posterior-precision mass, mass adaptation off.
- Seeds: 96001–96003 consumed (verified absent from protocols/preregistrations before use); data seed 2026083131+T for the local-level series.
- Status: engineering evidence with bounded preregistered benchmarks; not a public benchmark claim (shared machine; nutpie/NumPyro work counters are leapfrog proxies, labelled).
- Outcome: predictions 1–5 held. Eight Schools geometric-mean min-bulk ESS/s: owalnuts from_pymc GIL t1 2,261; cfunc t1 9,979; **cfunc t4 30,982 vs nutpie cores=4 27,754** (nutpie cores=1 12,537; NumPyro 1,857). Per-call 5.69 µs (GIL) → 1.25 µs (cfunc). ESS per exact fused call unchanged across transports (0.0336/0.0338). T=1000 + precision metric via cfunc: 6,072–6,196 ESS/s (t1), 20,791–22,471 (t4); WP15b numpy cell 2,455, same-run numpy control 4,302. All 15 sampled cells: zero divergences, max R-hat 1.0041, posterior means agree across cells. Tests 110 lib + 50 facade + 5 refresh + 13 pytest (incl. cfunc gaussian, zero-density wall vs truncated moments, gil-free-vs-GIL PyMC agreement); strict Clippy, fmt, `-D warnings` rustdoc clean. BridgeStan kept its direct impl (already GIL-free; preserves Stan's error messages); its 4 tests pass unchanged.
- Diagnostics: thread scaling cfunc t4/t1 ≈ 3.1×; nutpie compile 1.1–2.6 s per cell excluded from its sampling wall; ll cfunc per-call 1.35 µs.
- Artifacts: `integrations/python/bench/artifacts/gil-free-compare.json`; `integrations/python/{src/lib.rs, python/owalnuts/__init__.py, tests/test_adapters.py, bench/gil_free_compare.py, BENCH.md, README.md}`; `integrations/AUTODIFF-RESEARCH.md` §"GIL-free transport (WP18)"; facade `src/walnutpie.rs`, `tests/public_facade.rs`, `CHANGELOG.md`.
- Conclusion: supported — the nutpie gap on PyMC models was callback transport and is closed at equal per-gradient efficiency (parity at 4 cores, exact-vs-proxy work caveat noted); the structured-metric advantage compounds through the cfunc path (≈29× NumPyro's identity-metric ESS/s at T=1000, t4). Not supported — any strict claim against nutpie until a same-model exact-work three-way is run; any claim for non-numba-capable models.
- Next decision: preregister the same-`.stan`-file strict three-way (oWALNUTS-BridgeStan vs CmdStan vs nutpie); document `from_cfunc` in the release notes at the next version bump.

### WP19-FLAGSHIP-CRYPTO-SV-V1 — five-asset stochastic-volatility demo study
- Ordered time: 2026-08-31 (after WP18); sequence basis: git history.
- Protocol/config: `STUDIES/flagship_crypto_sv_v1/{PREREGISTRATION.md,protocol.json}`
  (SHA-256 in the study `CHECKSUMS.sha256`); kernel v10; owalnuts 0.1.0b2 with the
  budget/admission patch in `integrations/python/src/lib.rs`; standard SV model,
  unconstrained `[mu, a, s, h_1..h_T]`; OKX daily closes for BTC/ETH/XRP/BNB/SOL
  (T = 3153/3153/2433/1348/2159); cells native (Rust, paper adaptation v3,
  one-shot tridiagonalized-global + AR(1)+curvature path precision metric from a
  stage-A adapted-diagonal calibration), pymc (`from_pymc(gil_free=True)`, same
  metric), nutpie 0.16.8, NumPyro 0.21.0 (depth 12, 0.9); 4 chains, 1,000/3,000
  (owalnuts native+pymc 3 seeds; external references 1 seed).
- Seeds: pilot 97000 (BTC, non-evidence); evidence 97001–97003 consumed; no
  other namespace touched.
- Status: exploratory sampling evidence (demo study; preregistered with one
  pre-evidence amendment A1: depth 10→9 after a zero-callback admission
  failure at 1.54e9 worst-case vs the 1e9 research ceiling).
- Outcome: mixed, reported in full in the study README/RESULTS.md. Zero
  divergences in all 40 cells; cross-backend agreement for every healthy pair
  (worst z 2.29); oWALNUTS native fastest wall on 5/5 assets (e.g. XRP
  13.2–15.0 s vs nutpie 15.8 s / NumPyro 29.5 s; SOL 12.8–14.6 s vs 33.4/54.2 s).
  Primary health: owalnuts 3/3 on XRP (native+pymc) and SOL (native), 2/3 BNB;
  fails BTC/ETH at this budget (min primary ESS 99–398) where NumPyro passes
  (420–544). Global (a,s) ridge (corr ≈ −0.9) bottlenecks every backend;
  nutpie/NumPyro windowed adaptation extracts ~1.5–2× more global ESS at
  T≈3,150 than the one-shot metric. pymc arm stuck seeds ETH-97003,
  SOL-97001/97003 (R-hat 1.44–1.65, zero divergences) — start sensitivity of
  the frozen metric. Predictions: P1 falsified; P2, P4, P6 held; P3 held
  except stuck seeds; P5 half-held.
- Diagnostics: per-cell tables in `artifacts/RESULTS.md`; full numbers in
  `artifacts/summary.json`; raw draws hashed in `CHECKSUMS.sha256` (python
  .npz functionals committed; native .f64 draws are ~300 MB each and stay
  untracked, reproducible from the committed seeds).
- Artifacts: `STUDIES/flagship_crypto_sv_v1/**` (data, calibrations, runs,
  draws, figures, notebook builder); public notebook
  `integrations/python/examples/crypto_sv.ipynb` (executed) and
  `examples/crypto_sv.py`; funnel figure derives from the frozen WP14
  artifacts.
- Conclusion: supported claims — funnel correctness differentiator (from
  WP14), five-asset full-posterior sampling with universal agreement and zero
  divergences, fastest wall on all assets, one-line PyMC entry with GIL-free
  transport at native speed. Not supported — "passes all gates everywhere"
  (BTC/ETH global ESS at this budget) and any claim of global-ESS superiority
  over mature windowed adapters at T≈3,000+.
- Next decision: the global–path coupling (arrowhead line) is the mechanism to
  beat nutpie/NumPyro on the (a,s) ridge; a windowed/iterated calibration for
  the one-shot metric and start-robustness for the pymc arm are the cheap
  follow-ups. During piloting the study also exposed and fixed a Python
  package defect (structured-mass runs ignored the caller budget at
  admission); package tests 13/13.

### WP17-CANONICAL-V3-SCALE-NONCENTERED-V1 — full scale non-centering falsified; Appendix C adaptation passes on the real T=1000 target (V2-A)
- Ordered time: 2026-08-31, preregistered before sampling; oWALNUTS arms ~14:50–14:57 UTC; NumPyro arm N3 rerun completed ~16:0x UTC; analysis immediately after.
- Protocol/config: `STUDIES/canonical_v3_scale_noncentered_v1/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`, which is the hash authority for this entry). Targets: `polyscope-canonical-v2` (a=1) and `polyscope-canonical-v3-scale-noncentered` (`d_t = mu + sigma_x*eps_t`, log-Jacobian `(T-1) log sigma_x`; unit tests: round trip, Jacobian identity, 20-point central-difference gradient, v2 oracle parity 4/4 in Rust and JAX plus v3 consistency 4/4). Kernel `walnutpie-warmup-telemetry-tau0.6-m1-r2-e1-d3-v10` at `d8617a8`; paper mode `walnutpie-paper-adaptation-kquantile-gamma-v1` (v3 defaults). Arms per fixture (4 chains, 500/2,000, depth 8, identity initial diagonal, mass adaptation on): V2-I control (3 levels, max_error 1.0, DA 0.8, step search); V3-D (same, v3); V3-A (v3, Appendix C Δ=2/p_a=.95/Γ=.8, 8 levels, budgeted facade, exact admission 974,110,000, 200M runtime cap); V2-A secondary (v2 + Appendix C); N3 external (NumPyro 0.21 NUTS on the v3 density, depth 12, 1,000/2,000, target .9, jax 0.10.2/arviz 0.23.4 via `.venv-bench`). Fixtures sspd-10 (`005a5c7d…687ac`), sspd-11 (`2fff9766…83baad`), sspd-05 (`1d10f68e…8091d`); WP4b start rule; all 12 oWALNUTS preflights zero-callback (`artifacts/owalnuts-v1/preflight.json`).
- Seeds: consumed 95001 (sspd-10), 95002 (sspd-11), 95003 (sspd-05) for every arm; verified unused beforehand; no reserved seeds. Deviation 1: the first N3 process crashed in analysis (ArviZ 1.3 `from_dict` signature) after sampling sspd-10 but before any result was written or observed; rerun under `.venv-bench` re-consumed the same seeds (recorded in PREREGISTRATION.md before relaunch).
- Status: preregistered diagnostic evidence with external reference; negative for canonical-v3, positive (exploratory, single-seed) for V2-A.
- Outcome (gates: rank R-hat ≤1.01, bulk/tail ESS ≥400 on eight functionals, zero retained div/invalid/exhaustion, caps ≤1%):
  sspd-10 — every arm FAILS: N3 R-hat 3.269, 100% depth-12 saturation, 0 divergences, 1,544 s; V2-I 2.835/58.8% caps; V3-D 3.455/85.1% caps (chains never left the stiff sigma_x≈0.18 start); V3-A froze (8,000/8,000 retained transitions divergent+refinement-exhausted at depth 1, exact 2,048,000-call retained budget burned, chains bit-frozen at start); V2-A 2.167/40.2% caps, median depth 5, best ESS/call (1.49× V2-I).
  sspd-11 — V2-I PASS (1.0011, 841, 0.74% caps; reproduces WP12); **V2-A PASS (1.0020, min bulk 931, tail 1,767, 0.59% caps, 1.20× V2-I ESS/call; final δ 0.83–1.23, h 0.026–0.041, retained unrefined fraction 0.987)**; V3-D FAIL (3.335, 60.6% caps, sigma_x wandered to ≈0.98 against HalfNormal(0.1)); V3-A froze as above; N3 FAIL (2.844, 99.9% depth-12, 23 divergences).
  sspd-05 — V2-I PASS (1.0040, 582); V2-A PASS (1.0070, 548, 1.02× ESS/call; δ 0.94–1.24, h 0.087–0.123); V3-D FAIL (1.735, 77.3% caps); V3-A FAIL (2.364, 1,998 exhaustions); N3 FAIL narrowly (1.0114, bulk 708, 0 caps, median depth 9 vs 5 for v2, 58 s).
- Diagnostics: predictions P1 first clause, P2, P4 health clause, and P5 (V3-D side) falsified; P1 second clause, P6 held; P3 held only in letter (0 divergences because 100% of trajectories hit the depth-12 cap instead). Mechanism: v3 likelihood curvature in eps scales as sigma_x²·T²·c (top whitened eigenvalue 10^4–10^8 at the starts), so full scale non-centering relocates the funnel to moderate/large sigma_x; NumPyro needs median depth 9 at T=100 and saturates depth 12 at T=1000, so the failure is the parameterization, not the kernel. Full tables `STUDIES/canonical_v3_scale_noncentered_v1/artifacts/owalnuts-v1/results-table.md`; summary.json is machine-readable; raw draws hashed, not committed.
- Artifacts: `STUDIES/canonical_v3_scale_noncentered_v1/{PREREGISTRATION.md, protocol.json, README.md, Cargo.toml, Cargo.lock, src/, fixtures/, starts/, make_starts.py, numpyro_reference.py, analyze.py, checksums.py, run_owalnuts.cmd, run_numpyro.cmd, CHECKSUMS.sha256, artifacts/{owalnuts-v1/, numpyro/, owalnuts-v1-log.txt, numpyro-log.txt}}`.
- Conclusion: supported — (a) full scale non-centering of the innovations is falsified for this model family at T=100 and T=1000, for oWALNUTS and for NumPyro NUTS, with a quantified mechanism; (b) canonical-v2 a=1 remains the correct production coordinates; (c) the Appendix C warmup passes all gates on both healthy fixtures on the real target with a 1.02–1.20× ESS/call gain over dual averaging (single seed each — not yet confirmation); (d) sspd-10 remains unsolved by every tested Euclidean configuration and both parameterizations. Not supported: any claim that refinement substitutes for reparameterization in the stiff v3 region (V3-A froze), any V2-A performance claim beyond single-seed evidence.
- Next decision: (1) do not adopt canonical-v3; keep a=1 canonical-v2 for the product; (2) preregister a three-seed V2-A confirmation (candidate default warmup for hard targets) at 4×500/2,000 with the WP12 gates; (3) for sspd-10, the remaining candidates are position-dependent geometry (sigma_x-conditioned path-block refresh at slow-window boundaries) or *partial* scale centering a_s selected from data — new preregistrations, not extensions of this one; (4) retire full non-centering advice from the roadmap docs.


### WP21-CRYPTO-SV-V2 — closing the five-asset SV gates: pilot factorial, arm E, pooled extension, rebuild fallback
- Ordered time: 2026-08-31, after WP19/WP20; sequence basis: git history and PREREGISTRATION amendment chain A1–A7 (each dated before the runs it affects).
- Protocol/config: `STUDIES/flagship_crypto_sv_v2/{PREREGISTRATION.md, protocol.json}`; kernel v10; gates BYTE-IDENTICAL to v1 (primary: rank R-hat ≤ 1.01, bulk/tail ESS ≥ 400 on mu/h_T/mean_h, zero div/invalid/exhaustion, caps ≤ 1%; globals: R-hat ≤ 1.05, bulk ESS ≥ 100 on (a,s) — v1-preregistered weaker tier for the shared ridge; both views published, incl. a strict 1.01/400 auxiliary flag). No conditional runs anywhere: every cell samples the full joint T+3 posterior; "globals fixed" in one interim status message meant "globals-gate failures resolved", not conditioning.
- Seeds: pilot 98000/98004 (non-evidence, incl. arm-E addition A4 and arm-D arrowhead pilot); evidence 98001–98003; partner seeds 98011–98013 (A6 pooling); all verified fresh before use.
- Status: exploratory sampling evidence (demo study), with a preregistered pilot-selection phase and labeled extension/fallback tiers.
- Outcome: selection froze **arm E** (paper Δ=1.0, p_a=.95, Γ=0.95) from a six-arm BTC factorial (O/A/B/C/D/E; E's min{a,s,mean_h}=152 vs B 127, D 101, C 100 on the common seed). Evidence: at-budget (4×3,000) native XRP 3/3, ETH 2/3, BNB 2/3, SOL primary 3/3 (globals 0/3), BTC 0/3 (278–349 vs 400; v1 was 171–227). A 4×6,000 run is INADMISSIBLE under the 1e9 hard evaluation ceiling (first attempt failed closed at preflight; a broken env-var override produced bit-identical relabeled runs that were detected via the truthful retained-count field and deleted), so the labeled extension pools partner-seed runs into 8-chain rows: BTC native8c and pymc8c 3/3, ETH native8c 3/3, SOL native8c 3/3, XRP 3/3; BNB 2/3 (pair 98003 globals R-hat 1.06; its pymc8c twin passes). The pymc callable transport is thread-unsafe (shared PyTensor storage; threads=1, A2b); stuck cells (ETH-98001, SOL×3 — the v1 start-trapping mode) were rerun under the preregistered rebuild fallback `pymcB`, which healed all four (primary 4/4; SOL globals 1/3 residual). Zero divergences in all 82 oWALNUTS cells. Agreement 385/393 pairs at |z| ≤ 3 (8 exceedances ≤ 4.46 concentrated on SOL native-98013; none excluded). Strict-view: 1/78 cells passes 1.01/400 on the ridge pair; 0/10 references.
- Diagnostics: full per-cell tables `artifacts/RESULTS.md`, numbers + both gate views `artifacts/summary.json`; refresh/boundary telemetry per run; arm-D generalization pilots (ETH 168/130/527 @531k calls; SOL 125/76/1019 @545k) mark the arrowhead as the best per-call mechanism — next-phase research, no scorecard tier.
- Artifacts: `STUDIES/flagship_crypto_sv_v2/**` (hashes in `CHECKSUMS.sha256`; raw .f64 draws hashed, uncommitted); package changes in `integrations/python` (`sample(refresh=...)` exposing `sample_chains_structured_refresh` with `RefreshFailed` fallback semantics + telemetry; `from_pymc(thread_safe=True)` per-thread compiled functions; tests 16 passing); demo updated from `scripts/make_site_data.py`.
- Conclusion: supported — the user-set goal "passes on everything at the v1 budget" is met in full only within the labeled 2×-compute pooled tier plus the fallback path, and that is exactly how it is reported (at-budget rows first, tier labels, work multipliers, both gate views, residuals named: SOL-pymc globals 1/3, BNB one pooled pair at R-hat 1.06). Not supported — any claim that a single 4×3,000 configuration passes all gates on BTC/SOL; any global-ESS superiority over windowed adapters at T≈3,000 (unchanged from v1/WP19).
- Addendum (A8, 2026-09-01): quiet-machine paired re-measurement of all 30 native cells reproduced the evidence draws bit-for-bit (`artifacts-remeasure/verify.json`); walls changed 0.9–1.1× on most cells and 1.05–1.48× on BTC, so contention was not the main cause of v2's higher walls — arm E's ~1.7× call count is. Quiet walls replace contended ones in summary.json (`wall_contended` kept). The pymc thread-safe re-measurement phase did not run (job stopped externally) and remains the one-command follow-up; `from_pymc(thread_safe=True)` is shipped and tested.
- Next decision: develop the arrowhead line (best per call; needs ≥6-global generalization or a relaxed dimension check, Python exposure, and a preregistered confirmation); consider a globals-targeted second metric window; SOL-pymc globals and the BNB pooled pair remain the named open cells.
### WP22-POSTERIORDB-BENCH-V1 — default-settings posteriordb benchmark: Appendix C warmup rejected as default; DA arm behind CmdStan/nutpie on breadth
- Ordered time: 2026-09-01, preregistration committed (`4020d29`) before the first evidence cell; cells 20:46–22:52 local, strictly sequential; analysis immediately after.
- Protocol/config: `STUDIES/posteriordb_bench_v1/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). 17 posteriordb posteriors with reference draws (posteriordb `28f8d3d`; `radon_*`/`dogs` have no references and were replaced), 4 chains, 1,000/1,000, every sampler at its defaults: owalnuts-da (BridgeStan `StanTarget`, package-default tuning h0=0.1/depth 8/4 levels/delta 1, DA 0.8, adapted diagonal, 4 threads, uniform(-2,2) starts), owalnuts-paper (same + `PaperAdaptationConfig::default()`), CmdStan 2.39.0 via cmdstanpy 1.3.0 defaults, nutpie 0.16.8 BridgeStan backend defaults. Kernel v10. ArviZ 0.23.4 rank R-hat / bulk / tail ESS over the reference-draw parameter set; gates R-hat <= 1.01, ESS >= 400, zero divergences; agreement z against reference mean with both MCSEs.
- Seeds: 77101, 77102, 77103 for every arm (verified unused before freezing). Deviation 1: the driver process was killed externally during the `lotka_volterra` DA cells and relaunched; the interrupted cell was re-run from scratch (seeded, resumable). Deviation 2 (classification only): `lotka_volterra` paper 77101 is recorded as `timeout` although the harness had written an error result after 20 s and then hung at process exit for the remaining 45 min.
- Status: preregistered negative evidence for the Appendix C default question; preregistered breadth benchmark with external competitors (CmdStan, nutpie); all 204 cells present.
- Outcome: **Appendix C fails the preregistered decision rule** — geomean paper/DA min-bulk-ESS/gradient 0.077 (tail 0.061, ESS/s 0.067) over the 12 models complete on both arms; "at least as good" on 4/12 (two of those are models where both arms fail); paper passes 8/51 cells vs DA 26/51, CmdStan 34/51, nutpie 29/51. On nine models the paper arm froze all or some chains (exactly 128,000 calls, every retained transition a refinement exhaustion). Mechanism from `PaperAdaptationUpdate` telemetry: from uniform(-2,2) starts the first window's orbit energy ranges are 10^3–10^16, the K-quantile rule installs delta ~ 0 at transition 74 and never recovers (no orbit completes, unrefined fraction `None`, h stays 0.1) — the WP17 V3-A freeze mode on ordinary posteriors. Where it did work (noncentered eight schools 3/3, garch11 3/3) it was 0.90–0.95x DA per gradient. Head-to-head: owalnuts-da is 0.32x CmdStan and 0.25x nutpie on bulk ESS/gradient and 0.11x / 0.26x on bulk ESS/s (14 models, 0 wins); the one place oWALNUTS is ahead is `gp_pois_regr`, where both NUTS implementations diverge (4–175 per seed) and both oWALNUTS arms that ran had zero divergences (DA passes 2/3). DA-arm failure modes: step collapse to ~0.003 with 85–90% depth-8 caps on `diamonds`/`earnings` (CmdStan at depth 10 passes with 4x the gradients), `sblrc` R-hat 1.3, `accel_gp` R-hat 1.6; six `arma11` cells and four others died because `StanTarget` treats NaN log density / NaN-inf gradient as fatal where CmdStan and nutpie reject the proposal; two `lotka_volterra` starts were not evaluable (no start retry). Refinement was essentially never engaged (99% of retained leaves at level 0). Predictions: P1 falsified, P2 partially held (fewer but nonzero divergences on centered eight schools), P3 held (by freezing), P4 held. Agreement: every healthy cell of every arm within max |z| 3.3 of the reference.
- Diagnostics: `artifacts/results-table.md` (per-model table, Appendix C ratio table, head-to-head), `artifacts/summary.json` (every cell and metric), `artifacts/cells/*.json` (per-cell metrics incl. per-parameter z, step/delta telemetry, depth and refinement histograms), raw draws hashed and uncommitted.
- Artifacts: `STUDIES/posteriordb_bench_v1/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, run_posteriordb.py, checksums.py, CHECKSUMS.sha256, artifacts/{summary.json, results-table.md, cells/, measured_on.json, run-log.txt, run-log-2.txt}}`.
- Conclusion: supported — (a) `PaperAdaptationConfig::default()` is not safe as a default warmup: it needs a delta floor and/or a burn-in before the first K-quantile installation, or evaluable-start retries, and must be re-benchmarked after that; (b) at package defaults the oWALNUTS DA arm is behind CmdStan and nutpie on breadth, on gates and by 3–4x per gradient and ~9x per second, with depth 8 + diagonal metric and the BridgeStan-facade per-call overhead as the two identified causes; (c) the divergence-free advantage reappears only on `gp_pois_regr` in this set. Not supported: any breadth-throughput claim for oWALNUTS beyond the eight-schools strict track; any claim that refinement helps on these posteriors (it was not engaged).
- Next decision: (1) do not adopt Appendix C by default; add a delta floor / delayed first installation and start retries, then rerun this exact protocol (same seeds are consumed — use fresh ones); (2) make `StanTarget` map NaN density/gradient to a recoverable rejection and add CmdStan-style start retries in the harness; (3) raise the package default depth to 10 for parity and profile the facade + FFI per-call overhead on `hmm_example`/`arK` where walls are 10–30x CmdStan at equal gradient counts; (4) after (1)–(3), a v2 of this benchmark is the release gate for any breadth claim.
### WP23-POSTERIORDB-BENCH-V2 — v1 protocol rerun on the fixed sampler: integration failures gone, paper arm robust at parity, breadth gap to CmdStan unchanged
- Ordered time: 2026-09-02, preregistration committed (`cd184a5`) before the first evidence cell; 255 cells 01:40–02:41 local, one continuous strictly sequential run; analysis immediately after.
- Protocol/config: `STUDIES/posteriordb_bench_v2/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). The v1 model set (17 posteriordb posteriors, `28f8d3d`), 4 chains, 1,000/1,000, every sampler at its defaults. Arms: owalnuts-da (`sampler::Sampler`, `Tuning::default()` h0 0.5 / depth 10 / 4 levels / delta 1, `Metric::diagonal()`, DA 0.8, `Init::uniform()` retries, `ReplicatedStanTarget` on a non-`STAN_THREADS` BridgeStan build compiled fresh), owalnuts-paper (`PaperAdaptationConfig::default()` v4), owalnuts-stan-style (`WarmupConfig::stan_style(0.8)`), CmdStan 2.39.0 and nutpie 0.16.8 exactly as v1. Kernel v10, paper rule v4. Estimators and gates as v1.
- Seeds: 78101, 78102, 78103 (verified unused). Deviations: none in the driver; nutpie `lotka_volterra` 78102 panicked in `nuts-rs` (poisoned mutex) and killed the child before 78103, both recorded as `timeout_or_crash` cells, no rerun.
- Status: preregistered breadth benchmark, rerun of WP22 with the five post-v1 fixes; all 255 cells present.
- Outcome: cells passing all gates — owalnuts-da **32**/51 (v1 26), owalnuts-stan-style 32, owalnuts-paper 29 (v1 8), cmdstan 35 (v1 34), nutpie 27 (v1 29). Head-to-head geomeans (seed medians, 17 models): DA 0.233x CmdStan bulk ESS/gradient, 0.307x per second, **0.771x wall per gradient** (v1 ~3x); DA 0.230x nutpie per gradient but **1.108x per second** (9/16 models won); stan-style 0.319x / 0.450x CmdStan. Excluding the two models with frozen oWALNUTS chains (post hoc): DA 0.447x / 0.667x. Predictions: P3 (wall/grad <= 1.5x) and P5 (zero cells lost to fatal NaN or start failure; v1 lost 12) held; P1 missed by one cell (32 vs 33); P2 (>= 0.45x) not held (0.233); P4 (no frozen paper cell) not held — 5 frozen cells on `arma11` (all three arms, same chains from the same starts, `h -> 0`, 550–3,000 recoverable failures: the fatal-NaN fix turned v1's dead cells into frozen chains) and `lotka_volterra` (one chain per seed at `h ~ 1e-5` from ODE solver failures; stan-style's initial `delta` ramp survives one of the two seeds). Depth 10: `diamonds` 0/3 -> 3/3 on every oWALNUTS arm (8x ESS/gradient), `earnings` 0/3 -> 2/3 (12x). Build fix: 7–22x ESS/s on arK, hmm_example, eight schools at identical gradient counts. Appendix C v4 vs DA: geomean 0.995, at least as good on 13/17, loses kidiq, sblrc, hmm_drive_0, accel_gp — not a default (parity, not collapse). Only outright win: `diamonds` vs nutpie; gate wins vs CmdStan on noncentered eight schools (3/3 vs 1/3) and gp_pois_regr (up to 3/3 vs 0/3), zero oWALNUTS divergences on both and on centered eight schools/accel_gp/one_comp where NUTS diverges. On the healthy models oWALNUTS is 0.4–0.9x CmdStan per gradient (kernel-side gap of `adaptation_parity_v1`, unchanged). Agreement: every healthy cell within max |z| 3.5.
- Diagnostics: `artifacts/results-table.md` (per-model table, Appendix C ratios, head-to-head incl. wall/grad and outright wins, v1-vs-v2 per model, predictions), `artifacts/summary.json`, `artifacts/cells/*.json` (per-parameter z, per-chain exhaustions/step/delta, depth and refinement histograms), raw draws hashed and uncommitted.
- Artifacts: `STUDIES/posteriordb_bench_v2/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, run_posteriordb.py, checksums.py, CHECKSUMS.sha256, artifacts/{summary.json, results-table.md, cells/, measured_on.json, run-log.txt, run-log.err.txt}}`.
- Conclusion: supported — (a) the integration layer is fixed: no oWALNUTS cell dies on NaN/inf or an unevaluable start, and per-gradient wall is below CmdStan's; (b) depth 10 and the v4 paper rule remove the v1 regression failures; the paper rule is safe but not better than DA; (c) the breadth gap is now entirely kernel-side: 0.4–0.9x CmdStan per gradient on healthy models plus two models where chains freeze from starts where every leaf fails (`arma11`, `lotka_volterra`), which NUTS's reject-and-shrink survives. Not supported: the breadth-throughput claim (the release gate P1–P3+P5 is not met: P1 by one cell, P2 by a factor of two); any claim that refinement helps here.
- Next decision: (1) the frozen-chain mode — a start where every leaf is a recoverable failure at every refinement level and `h -> 0` — needs a kernel rule (treat an all-exhausted transition as a NUTS reject with a step shrink, or fall back to `delta = divergence threshold` until the first completed orbit, as the stan-style ramp partly does) and a targeted study on `arma11`/`lotka_volterra` starts; (2) the per-gradient gap on healthy models (U-turn/selection rules, reverse-check overhead) is the remaining efficiency target; (3) v3 of this protocol after (1) is the release gate for a breadth claim.
### WP24-FREEZE-MODE-V1 -- the frozen-chain mode is WALNUTS' two-sided leaf test; Stan's one-sided rule during warmup is the fix and the new `sampler` default
- Ordered time: 2026-09-02 on `wt/freeze-mode`; diagnosis and rule development on the v2 seeds of `arma11`/`lotka_volterra` (not preregistered), then the arm table and predictions committed (`55e3820`) before the table ran; the funnel side check (amendment 1) before the table.
- Protocol/config: `STUDIES/freeze_mode_v1/README.md` section 3. The v2 starts (`uniform_starts`, seeds 78101-78103), 4 chains, 1,000/1,000, `Sampler` defaults plus one of nine arms; ESS over unconstrained coordinates; "frozen" = no run of 50 transitions with >= 45 position changes. Side checks: 10-D funnel 4 x 2,000/20,000 at sampler defaults (tail mass vs 0.0478), centered Eight Schools 4 x 1,000/1,000.
- Seeds: 78101-78103 (the v2 cells, reproduced bit for bit). Deviations: the table job was stopped once by the host and resumed (resumable driver); the six `step-floor` cells were redone after a fix to the floor (the final averaged step was not floored); every other cell ran once.
- Status: diagnostic study with preregistered confirmation; defaults flipped in the labelled final commit.
- Outcome: mechanism -- the seven frozen `arma11` chains start at log density -4.5e19 to -1.8e115 (44 % of uniform(-2,2) starts are past |logp| 4e18 on this model); every downhill leapfrog from there has an astronomically negative energy error, which WALNUTS' two-sided `|H_end - H_start| <= delta` turns into an exhaustion at the initial leaf, and the coarse-endpoint statistic (0) then drives dual averaging to `h ~ 1/|grad|` (1e-20 to 1e-68) where `q + h v == q` and the kinetic energy is below the ulp of `U`: a stable equilibrium at statistic ~0.8 with no position change. CmdStan escapes all seven starts through its one-sided, transition-relative `H - H_0 > 1000`. `lotka_volterra`'s slow chains are numerical noise (rk45 tolerance x 1e4 residual scale = energy jumps of ~50 independent of h) on starts CmdStan rejects at init. Fix -- `ExhaustionRule::AcceptUnlessDivergent` (Stan's test with rises below `|H_0| * 2^-40` counted as zero, Stan's statistic for the kept leaf) applied to warmup only (`WarmupConfig::with_warmup_exhaustion_rule`): `arma11` 0 frozen of 12 on all seeds, min bulk ESS 1,290-1,460 (baseline 4-7), escapes at warmup transitions 6-85; `lotka_volterra` 78101/78103 pass (900/842; baseline 78103 was 6.8), 78102 crawls under every arm. Two-sided arms (`baseline`, `exhaust-accept`, `mean-accept`, `stan-style`, `step-floor`) freeze exactly as v2; the rule combined with Stan's statistic or preset re-freezes chains. Funnel at sampler defaults: retained-phase acceptance of exhausted leaves -- one-sided (z -7.2) or the existing two-sided `AcceptBelowDivergenceThreshold` (z -7.3) -- loses the neck; warmup-only matches the baseline (0.0214 +- 0.014 vs 0.0236 +- 0.014). Centered Eight Schools min bulk ESS 104 vs 21. Predictions: P1, P2, P4, P6 held; P3 half held, P5 not.
- Diagnostics: `artifacts/results-table.md`, `artifacts/summary.json`, `artifacts/table/*.json` (per chain: escape transition, final step, trace summaries), `artifacts/checks/*.json`, `cmdstan/out/trace-*.csv` (Stan's warmup from two frozen starts), `tests/freeze_mode.rs` (synthetic wall target: pin and escape).
- Artifacts: `STUDIES/freeze_mode_v1/{README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, src/bin/checks.rs, run_table.sh, analyze.py, build_models.py, cmdstan/*.stan, cmdstan/out/*.init.json, artifacts/}`.
- Conclusion: supported -- the freeze is a kernel-rule mode (two-sided leaf test + coarse statistic), not a start-check, momentum, level-cap or step-floor problem; Stan's one-sided rule during warmup removes it on every cell with no measured loss and no change to retained-phase behaviour. Not supported: any accepting exhaustion rule for retained draws (funnel bias at four levels), the rule with Stan's statistic or preset.
- Next decision: v3 of the posteriordb protocol on the new default (breadth confirmation; `arma11` expected 3/3); the funnel at sampler defaults (four levels, `h0 = 0.5`) deserves its own look; `lotka_volterra` starts on the ODE failure boundary need an init-time treatment (reject a start whose ODE solution is at `max_num_steps`) that this study did not attempt.
### WP25-POSTERIORDB-BENCH-V3 — the v2 protocol on the WP24 warmup default: arma11 and lotka_volterra recovered, nothing else moved, breadth gate still missed on hmm_drive_0's modes and sblrc's step
- Ordered time: 2026-09-02, preregistration committed (`14cdb4b`) before the first evidence cell; 204 cells 14:41–15:34 local, one continuous strictly sequential run; analysis immediately after.
- Protocol/config: `STUDIES/posteriordb_bench_v3/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). Identical to WP23 (v2): 17 posteriordb posteriors `28f8d3d`, 4 chains, 1,000/1,000, every sampler at its defaults, v2 gates and estimators; differences: harness rebuilt against `src/` at `3e2da75` so `Adaptation::DualAveraging` applies `DEFAULT_WARMUP_EXHAUSTION = AcceptUnlessDivergent` to warmup transitions (kernel revision v10 unchanged; retained rule `Stop`), arms owalnuts-da, owalnuts-stan-style (two-sided `stan_style(0.8)` preset used as given — the control), cmdstan 2.39.0, nutpie 0.16.8 (paper arm dropped: 0.995x DA in v2), every model compiled fresh (v2 libraries gone with their worktree).
- Seeds: 79101, 79102, 79103 (verified unused). Deviations: nutpie `lotka_volterra` 79101 panicked in `nuts-rs` (same failure as v2's 78102) and killed the child before the other two seeds; all three `timeout_or_crash`, no rerun.
- Status: preregistered breadth confirmation of WP24; all 204 cells present; no oWALNUTS cell errored.
- Outcome: cells passing all gates — owalnuts-da **35**/51 (v2 32), owalnuts-stan-style 29 (v2 32), cmdstan 37 (v2 35), nutpie 31 (v2 27). Head-to-head geomeans (seed medians, 17 models): DA **0.344x** CmdStan bulk ESS/gradient (v2 0.233), 0.492x per second, **0.751x wall per gradient**; 0.296x nutpie per gradient, **1.350x per second** (10/16 models). WP24 effect: `arma11` 0/3 -> **2/3**, zero frozen DA chains (v2: 7 of 12), 424x ESS/gradient, escapes as 114–455 one-sided warmup slides per cell; the failing seed's chain 3 escapes the pin but ends warmup at `h = 5e-8` with 794 depth-cap orbits (a crawl, not a freeze); the two-sided stan-style control freezes 7 of 12 chains on the same seeds. `lotka_volterra` 1/3 -> **3/3** (0.95x CmdStan per gradient). The models the rule cannot touch reproduce v2 within seed noise (DA v3/v2 ESS/gradient 0.85–1.32 on 11, one_comp 2.09, centered eight schools 0.41 where every arm fails) except `hmm_drive_0` (2/3 -> 1/3, 0.02x: one chain in a second HMM mode on two seeds vs one in v2; zero exhaustions, identical gradient counts and steps — a start-mode coin flip, CmdStan's own ratio 1.93) and `sblrc` (1/3 -> 0/3, 0.67x: bulk ESS 205–280 vs 202–425 at the collapsed DA step 0.003, the v2 finding). Predictions: P1 (>= 33 cells: 35), P2 (`arma11` >= 2/3), P5 (wall/grad <= 1.0x: 0.751) held; P3 (>= 0.40x per gradient: 0.344; 0.452 without `hmm_drive_0`, 0.516 without `sblrc` too, post hoc) and P3b (>= 0.45 excluding `lotka_volterra`: 0.323, the exclusion now removes a 0.95x model) not held; P4 (no model < 0.8x v2) not held on `hmm_drive_0`, centered eight schools, `sblrc`. Wins: `one_comp` outright vs nutpie; gates vs CmdStan on noncentered eight schools (3/3 vs 2/3) and `gp_pois_regr` (2/3 and 3/3 vs 0/3), zero oWALNUTS divergences on both. Healthy-model efficiency 0.47–0.88x CmdStan per gradient, `earnings` 0.23x, `sblrc` 0.06x (`adaptation_parity_v1` gap, unchanged). Agreement: every passing cell within max |z| 4; one `one_comp` cell per arm at 4.0–4.6.
- Diagnostics: `artifacts/results-table.md` (per-model table, head-to-head incl. wall/grad and outright wins, v2-vs-v3 per model, predictions), `artifacts/summary.json`, `artifacts/cells/*.json` (per-parameter z, per-chain steps/exhaustions, depth and refinement histograms, the recorded warmup exhaustion rule), raw draws hashed and uncommitted.
- Artifacts: `STUDIES/posteriordb_bench_v3/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, run_posteriordb.py, checksums.py, CHECKSUMS.sha256, artifacts/{summary.json, results-table.md, cells/, measured_on.json, run-log.txt, run-log.err.txt}}`.
- Conclusion: supported — (a) the WP24 default removes the frozen-chain mode on the full protocol (zero frozen DA chains on 51 cells; `arma11` and `lotka_volterra` recovered) at no measured cost elsewhere: the rule is confirmed as the default; (b) the remaining `arma11` failure is a crawl at `h ~ 1e-8` after the escape, a second start-region mode that NUTS also fails on one seed here. Not supported: the breadth-throughput claim — the release gate (P1 + P3 + P5) is missed on P3 by `hmm_drive_0`'s multimodal start draw (seed-level noise on this model, 0.02–1.93x between versions across arms) and `sblrc`'s DA step collapse, not by any kernel change; the geomean over the models where nothing is seed-pathological (0.45–0.52x) is where v2 already stood.
- Next decision: (1) the `sblrc`/`earnings` step collapse under dual averaging (0.003 with 300–450 depth caps; the stan-style preset gets 6–10x per gradient with its initial step search and metric prior) is the largest fixable per-gradient item on this set — a targeted adaptation study; (2) `hmm_drive_0` is a start-mode question (four uniform(-2, 2) starts on a label-switching HMM) that should be reported per chain or given a start rule before it decides a breadth gate; (3) the post-escape crawl (`arma11` 79103 chain 3 here, `lotka_volterra` 78102 in WP24) — `h -> 1e-8` with depth-cap orbits after leaving the pin — is the next kernel-rule target; (4) the kernel-side gap on healthy models (0.5–0.9x CmdStan per gradient) remains the efficiency target.

### WP30-KERNEL-GAP-V1 -- the per-gradient gap to NUTS on ordinary posteriors is leaves per orbit under the endpoint U-turn statistic; `MomentumSum` recovers 0.77x -> 0.90x at matched step, metric and starts
- Ordered time: 2026-09-02 on `wt/kernel-gap`; matched-condition instrumentation study, not preregistered (attribution and candidate generation; no default flipped).
- Protocol/config: six posteriordb models (`earnings`, `kidiq`, `mesquite`, `nes2000`, `garch11`, `arK`; `28f8d3d`, BridgeStan without `STAN_THREADS`). CmdStan 2.39.0 once per model (defaults, seed 84200); each chain's adapted `stepsize__`, inverse metric and first retained draw (unconstrained via BridgeStan) are the fixed step, `M^-1` and start of that chain in every arm. No warmup, 4 x 2,000, depth 10. Arms: reference NUTS (`examples/support/reference_nuts.rs`, Stan `base_nuts` 2.21+), oWALNUTS through `Sampler` (`Adaptation::None`, `Metric::fixed_diagonal`, four levels, `delta = 1`, cache) with `Endpoints` / `EndpointsWithCross` / `MomentumSum`, `delta = 1000`, one level + `AcceptUnlessDivergent` (with and without `MomentumSum`), four levels + `AcceptUnlessDivergent`. Metric: minimum bulk ESS over unconstrained coordinates per gradient, decomposed as ESS per orbit x 1 / (leaves per orbit x gradients per leaf); per transition the stop cause, orbit size and the selected/initial state indices (new diagnostics). Ratios to the reference on the same seed, seed medians, geometric mean over models.
- Seeds: 84201-84202 (fresh); CmdStan 84200; funnel 252601-252603 (fresh). Deviations: none; 96 cells ran once (`artifacts/table-run.log`).
- Status: instrumentation study; no default flipped; no new kernel rule (the existing opt-in `UTurnRule::MomentumSum` is the prototype).
- Outcome: the reference NUTS reproduces CmdStan's own run (leapfrogs per orbit 158/29/49/50/11/33 vs 155/29/50/49/11/33). Default kernel geomean 0.77x the reference per gradient = 0.46x ESS per orbit x 1.66 orbits per gradient, with leaves per orbit 0.60x (0.26x earnings, 0.49x kidiq, 0.67x nes2000, 0.70x garch11, 0.82x mesquite, 0.92x arK) and gradients per leaf 1.01x; 41-69 % of orbits end in the recursive endpoint U-turn. `delta1000`, `levels1`, `accept` are within 1 % of the default (refinement engages on 0.1-0.7 % of leaves; non-leaf gradients 0.04-0.19 per orbit); `EndpointsWithCross` 0.69x. `MomentumSum`: leaves 0.97x (0.95-0.98 per model), ESS per orbit 0.88x, ESS per gradient **0.90x** (0.84-0.96); `earnings` 40 -> 151 leaves per orbit (reference 158). Control `levels1+rhosum` 0.95x per gradient, 0.997x leaves, selected-state relative displacement 0.51-0.56 as the reference: no selection difference. The 0.90 -> 0.95 residual is refinement's reverse-coarser orbit stops (3-7 % of orbits at `delta = 1`). `uturn_default_v1` measured `MomentumSum` at 1.12x geomean on these six under the default warmup; the 1.06x/0.78x that rejected it came from the other eleven models under the unit-floored metric or the depth cap. Funnel (3 seeds pooled): `MomentumSum` z -0.5 (paper tuning) and -0.3 (sampler defaults); `Endpoints` +2.2 and -3.8 on these seeds (the defaults result differs from WP28's three seeds; batch-means s.e. underestimated).
- Diagnostics: `artifacts/results-table.md`, `artifacts/summary.json`, `artifacts/cells/*.json` (traces summarised), `artifacts/cmdstan/*.json`, `artifacts/funnel/*.log`.
- Artifacts: `STUDIES/kernel_gap_v1/{README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, build_models.py, cmdstan_reference.py, run_table.py, artifacts/}`; `examples/support/reference_nuts.rs` (shared reference NUTS with orbit statistics; `examples/kernel_efficiency.rs` uses it), `examples/funnel_kernel_options.rs` (`--seed`, `--sampler-defaults`); `src/kernel.rs` (`Span::{states, selected_offset}`, `TransitionDiagnostics::{orbit_states, selected_index, initial_index}`), `src/walnutpie.rs` (the same on the public `TransitionDiagnostics`; `WorkTotals::{accepted_forward_micro_steps, refinement_level_built}`). Counters only: fingerprints unchanged, every test green with and without `research`.
- Conclusion: supported -- on healthy posteriors under a correct metric the kernel-side gap is the endpoint U-turn statistic ending orbits early (leaves per orbit), not gradients per leaf and not selection; `MomentumSum` removes it to within the refinement residual. Not supported: the WP26 reading that "the gap on healthy models is not the U-turn rule" (it was measured under the warmup that hides it).
- Next decision: re-decide `UTurnRule::MomentumSum` as the sampler default jointly with `DiagonalMetricRegularization::Stan` on the v3 protocol with fresh seeds (the two are one finding; expected 1.15-1.2x on the healthy models and the `earnings` gate under `reg`); the reverse-coarser residual is a `delta` adaptation question (paper K-quantile rule), not a kernel rule.

### WP28-FUNNEL-DEFAULTS-V1 — the sampler defaults halve the funnel's tail mass; eight refinement levels fix it at no measured cost and become the default
- Ordered time: 2026-09-02 on `wt/funnel-defaults`; preregistration, protocol and driver committed (`ef62362`) before the first evidence cell; 81 cells in one strictly sequential run of about two minutes; summary immediately after; default applied in the labelled final commit.
- Protocol/config: `STUDIES/funnel_defaults_v1/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). 10-D Neal funnel (paper definition, starts `omega in {-3, -1, 1, 3}`), 4 chains x 2,000 / 20,000, `Sampler` at its 0.2.0 defaults (adapted diagonal, dual averaging 0.8 with the WP24 warmup rule, `h0 = 0.5`, depth 10, four levels, `delta = 1`, cache, worst-case admission) plus one override per arm: `defaults`, `levels8`, `delta0.5`, `delta0.25`, `levels8+delta0.5`, `paper-4`, `paper-8`, `stan-style`, `nuts-1` (one level, control). Cost cells on the noncentered Eight Schools (strict-track density and starts) and a 100-D Gaussian (`Init::uniform`), 4 x 1,000 / 1,000, same arms. Tail-mass `z` from the ArviZ MCSE of the indicator; cost = mean bulk ESS per retained call, seed median, vs `defaults`. Decision rule: cheapest arm with |z| <= 2 on all three seeds and >= 0.9x on both cost targets; else cheapest unbiased; else README note + `Tuning::funnel()`.
- Seeds: 82101–82103 (verified unused). Deviations: none; the `stan-style` 82103 funnel cell errored (nonfinite target value on the non-recoverable path after the preset's `delta = 1000` fast phase) and is reported as a failure of the arm, not rerun.
- Status: preregistered grid; default changed in the final commit.
- Outcome: `defaults` biased low on two seeds (0.0203 z −3.5, 0.0242 z −3.75, 0.0625 z +0.3; 2,933 retained exhaustions, 54 divergences over three seeds) — P1 held. `levels8` within |z| <= 2 on every seed (0.0412 −0.34, 0.0346 −1.43, 0.0897 +1.00; 113 exhaustions, 8 divergences) at 1.08x the funnel calls, **1.05x** Eight Schools ESS/call, **1.00x** Gaussian (two seeds call-for-call identical) — P3, P6 held. `levels8+delta0.5` also unbiased (−1.88, −0.80, −0.68), zero divergences, better `omega` R-hat (<= 1.017 vs 1.011–1.037) at 0.74x funnel calls but 0.79x on the Gaussian. `delta0.5` / `delta0.25` alone worsen the bias (11,000–12,000 exhaustions; two seeds short each). `paper-4` biased; `paper-8` from `h0 = 0.5` missed one seed (0.0293, z −2.72) and paper mode is 0.39–0.41x on the Gaussian. `stan-style` biased with 2,132 divergences on one seed and an errored cell. `nuts-1` shows the NUTS failure (two seeds with no draw below −5; 78,469 exhaustions) — P2 held. Caveat: at eight levels one chain per seed still adapts to `h` = 0.007–0.019 and either never leaves or never enters the neck; the pooled estimate is right, the `omega` R-hat is 1.01–1.04 and its bulk ESS ~100 per 80,000 draws — the default is unbiased on the funnel, not efficient on it.
- Diagnostics: `artifacts/results-table.md`, `artifacts/summary.json`, `artifacts/cells/*.json` (per-chain tail mass, final `h` and `delta`, ESS on every coordinate, health counts), `artifacts/run-log.txt`.
- Artifacts: `STUDIES/funnel_defaults_v1/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, run_grid.sh, CHECKSUMS.sha256, artifacts/}`.
- Conclusion: supported — (a) the 0.2.0 sampler defaults are biased on Neal's funnel (half the tail mass) because four halvings of the adapted step cannot enter the neck; (b) `max_refinement_levels = 8` removes the bias on every seed and is free on the ordinary targets (the cap never engages there), so it is the new `Tuning::default()`. Not supported: a lower `delta` as a fix (it makes the four-level bias worse); `Adaptation::Paper` from the sampler's `h0 = 0.5` as an unbiased default (one seed missed, and 0.4x on the Gaussian); the `stan_style` preset on this target.
- Next decision: (1) the per-chain step collapse on the funnel under dual averaging (one chain per seed at `h ~ 0.01`, R-hat 1.01–1.04 at eight levels) is the remaining funnel defect at the defaults — the same mode as the `sblrc`/`earnings` collapse in WP25 and the `arma11` post-escape crawl in WP24, so one adaptation study should address all three; (2) `levels8+delta0.5` is the funnel-efficient setting and should be re-measured on posteriordb before it is offered as more than a README note; (3) the hand-written funnel example should map every nonfinite density to the recoverable path, as BridgeStan does.
### WP27-STEP-COLLAPSE-V1 -- the default step collapse is the unit-variance metric floor, the `arma11` crawl is a start CmdStan cannot leave either; Stan's regularisation stays opt-in
- Ordered time: 2026-09-02 on `wt/step-collapse`; diagnosis on the v3 seeds 79101-79103 of `sblrc`, `earnings`, `arma11` with per-transition warmup telemetry and CmdStan `save_warmup` traces from the same starts (not preregistered), then the remedy table and predictions committed (`4546d30`) before the first table cell.
- Protocol/config: `STUDIES/step_collapse_v1/PREREGISTRATION.md`. Seven posteriordb models (`sblrc`, `earnings`, `diamonds`; `arma11`; controls `kidiq`, `mesquite`, `nes2000`), 4 chains, 1,000/1,000, `Sampler` defaults (WP24 warmup rule) plus one of ten arms through `Adaptation::Custom`; CmdStan 2.39.0 defaults from the same constrained starts. Metric: seed median of min bulk ESS per gradient over unconstrained coordinates (ArviZ over constrained parameters for CmdStan; rank-based, transform-invariant); v3 gates.
- Seeds: 80101-80102 (fresh). Deviations: none; every cell ran once (41 min, no failure).
- Status: diagnostic study with preregistered remedy table; no default flipped.
- Outcome: mechanism (a) -- the `v10` diagonal regularisation `(n/(n+5)) var + 5/(n+5)` floors every variance at 0.0099 for `n = 500`; `sblrc`'s coefficients have posterior variance 1.07e-5 (CmdStan's inverse metric), so the installed metric is 1,000x too wide, the momentum scale 30x too large in the stiff directions and dual averaging, whose statistic sits exactly on 0.80 throughout, correctly holds `h` at 0.003 = CmdStan's 0.10 / 30. `earnings`: three of five variances (1.6e-4 to 5e-4) floored to 0.01, `h` 0.003 vs 0.017, 9-14 % depth-cap draws. `diamonds` is not a collapse case at depth 10. Stan's statistic, Stan's `init_stepsize` at every window, the delta ramp, `mu = ln 10h`, a floor at half the search result and a 4x/10x within-stream shrink bound all leave `h` at 0.003-0.007 on `sblrc`; `DiagonalMetricRegularization::Stan` alone gives `h` 0.10-0.12 and 9x ESS/gradient. Mechanism (b) -- `arma11` 79103 chain 3 (start log density -3.1e84) slides under the WP24 rule to energy 3.7e5 by transition 250 and then crawls at `h` 1e-8 (statistic on target, 700-leaf orbits, position moving 1e-6 per transition); CmdStan from the same start reaches -3.67e5 at iteration 250 and ends at `h` 1.7e-6 with 832 treedepth-10 draws, R-hat 1.6, ESS 7 -- the same cell. No warmup option moves it (ramp bit-identical; statistic/regularisation/preset end at 5e-8 to 2e-6); a step floor pins the sliding chains. (c) -- `diagnostics::ChainDisagreement` (leave-one-chain-out max R-hat, the chains whose removal alone brings it under 1.01) added to `Summary`. Table (seed-median x baseline): `reg` sblrc 9.7x (gates 1/2, R-hat 1.011), earnings 1.9x but gates 0/2 (min bulk ESS 164-179, R-hat 1.02: at CmdStan's step and metric the WALNUTS orbit is 49 leaves against NUTS's 163, 70 % recursive U-turns, per-draw ESS a quarter of CmdStan's), diamonds 0.90x, arma11 2.9x, controls 1.08x geomean (kidiq 1.06, mesquite 0.97, nes2000 1.24); `reg+research` 3.0x on the collapsing three, 0.98x controls, earnings still 0/2; `stan-style` 1.32x overall, 0.91x controls; non-metric arms 0.72-1.09x with `h` unchanged on the collapsed models; `shrink:10` freezes one `arma11` chain per seed (negative control held). Predictions: P2, P4, P5 held; P1 and P3 half (sblrc yes, earnings 1.9x/0.48x CmdStan).
- Diagnostics: `artifacts/results-table.md`, `artifacts/summary.json`, `artifacts/cells/*.json` (table cells and `diag-*` diagnosis cells, traces stripped), `artifacts/cmdstan-cells/*.json`, `artifacts/table-run.log`; `trace_summary.py` reproduces the per-window tables of README section 1 from a regenerated cell.
- Artifacts: `STUDIES/step_collapse_v1/{README.md, PREREGISTRATION.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, build_models.py, cmdstan_trace.py, trace_summary.py, run_cell.sh, run_table.py, artifacts/}`; `src/walnutpie.rs` (`WarmupConfig::with_step_floor_relative_to_search`, `with_max_window_shrink`, opt-in), `src/diagnostics.rs` (`ChainDisagreement`, `Summary::chain_disagreement`, `RHAT_DISAGREEMENT_THRESHOLD`).
- Conclusion: supported -- (a) is the metric regularisation's absolute floor, not the step rule, the search, the schedule or a chain asymmetry; Stan's regularisation with the WP24 rule removes it on `sblrc` and lifts `earnings` to CmdStan's step, but `earnings` then fails R-hat through a retained-kernel orbit-length gap at that metric, so the preregistered flip rule is not met and the option stays opt-in (recommended for regressions with coefficient scales below ~0.1). (b) is a start both samplers crawl on. Not supported: any step-side remedy for (a); any floor on the warmup step (incompatible with the WP24 escape).
- Next decision: the retained-kernel orbit length under a well-scaled metric on correlated regressions (`earnings` under `reg`: 49 vs 163 leaves at the same `h`) is the blocker for making Stan's regularisation the default and the largest remaining kernel-side gap; a scale-free regularisation (toward the window's median variance) as an alternative default candidate; a cross-chain restart or an init bound for the `arma11`-type start.
### WP26-UTURN-DEFAULT-V1 — `UTurnRule::MomentumSum` as the `sampler` default: rejected by the preregistered rule (1.06x geomean, 0.78x worst model, no gate gain); stays opt-in
- Ordered time: 2026-09-02, preregistration committed (`2095491`) before the first evidence cell; 153 posteriordb cells 19:34–20:17 local, then 18 funnel and 9 Eight Schools cells, one continuous strictly sequential run; analysis immediately after.
- Protocol/config: `STUDIES/uturn_default_v1/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). The `posteriordb_bench_v3` protocol (17 posteriors `28f8d3d`, 4 chains, 1,000/1,000, `Tuning::default()`, `Metric::diagonal()`, `Adaptation::default()` with the WP24 warmup rule, `Init::uniform()` starts shared across arms per seed, v3 gates and ArviZ estimators, models compiled fresh without `STAN_THREADS`) restricted to oWALNUTS arms differing only in `KernelOptions::u_turn`: owalnuts-da (`Endpoints`, current default), owalnuts-da-rhosum (`MomentumSum`), owalnuts-da-cross (`EndpointsWithCross`, reported). CmdStan cited from v3. Side checks: Neal's funnel tail mass 4 x 2,000/20,000 at the paper tuning and at the sampler defaults, three seeds pooled; the `eight_schools_v9_rebench_v1` strict track, three seeds x three repetitions. Decision rule: flip iff geomean rhosum/endpoints min-bulk-ESS/gradient >= 1.10 over 17 models (C1), no model < 0.85 (C2), funnel |z| <= 2 under both tunings (C3), Eight Schools ESS/call ratio >= 0.9 (C4).
- Seeds: 80101, 80102, 80103 (verified unused). Deviations: none; no restart; cross arm run on all models.
- Status: preregistered decision study; all 180 cells present; no cell errored.
- Outcome: **C1 not held — 1.064**; **C2 not held — centered eight schools 0.78, diamonds 0.80, gp_pois_regr 0.846**; C3 held at the paper tuning (rhosum z +1.03, endpoints +0.90) and **not held at the sampler defaults (rhosum −3.39, endpoints control −11.24)**; C4 held (1.077). Cells passing: endpoints 38/51, rhosum 37/51 (loses `earnings` 80103 and `gp_pois_regr` 80101 to R-hat 1.012–1.013 and `hmm_drive_0` 80103 to a second-mode chain; gains `accel_gp` 80103 and `lotka_volterra` 80101), cross 37/51. Per model rhosum/endpoints: 1.18–2.14x on kidiq, garch, nes, one_comp, hmm_drive_0, lotka_volterra (gained by more ESS at equal or more gradients: garch 1.61x ESS at 1.26x gradients, accel_gp 2.26x at 2.35x); 0.78–0.93x on centered and noncentered eight schools, diamonds, gp_pois_regr, arma11, arK, earnings (diamonds/earnings: 0.73x gradients, halved depth caps, min ESS 0.57–0.68x); within ±10 % on mesquite, hmm_example, sblrc, accel_gp. Gradients geomean 1.071x, ESS/s 1.025x, sampling-only ESS/gradient 1.018x. vs CmdStan (v3 medians): endpoints 0.434x, rhosum 0.462x. Cross arm 0.811x (collapses on lotka_volterra 0.26, accel_gp 0.34). Endpoint arm reproduces v3 within seed noise on 13 healthy models (0.89–1.17); `hmm_drive_0` 91.7x (no second-mode chain drawn), `lotka_volterra` 0.25x (80103 on the `rk45` boundary, frozen chain in both arms). Funnel paper tuning: every rule |z| <= 1.03, rhosum at 0.97x calls and 1.14x omega ESS/call. Funnel sampler defaults: biased under every rule (endpoints 0.0171, rhosum 0.0313, cross 0.0396 vs 0.0478; 800–2,900 retained exhaustions per seed) — `freeze_mode_v1`'s single-seed z −1.70 (s.e. 0.014) was underpowered. Eight Schools strict: endpoints 0.01734 (reproduces v9's 0.01695–0.01762), rhosum 0.01867 (1.2x ESS at 1.11x calls), cross 0.01639; bit-identical repetitions, all healthy.
- Diagnostics: `artifacts/results-table.md` (per-model table with per-seed ratios, funnel and Eight Schools tables, criteria), `artifacts/summary.json`, `artifacts/cells/*.json`, `artifacts/funnel/*.json` (batch means), `artifacts/eight_schools/*.json`; raw draws hashed, uncommitted.
- Artifacts: `STUDIES/uturn_default_v1/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, src/bin/funnel.rs, src/bin/eight_schools.rs, run_uturn.py, checksums.py, .gitignore, CHECKSUMS.sha256, artifacts/{summary.json, results-table.md, cells/, funnel/, eight_schools/, measured_on.json, run-log.txt, run-log.err.txt}}`.
- Conclusion: not supported — `MomentumSum` is not a uniform improvement on the posteriordb set (a per-model coin, 0.78–2.14x, 1.06x geomean inside seed noise, one cell fewer) and is not made the default; `Tuning::default()` keeps `UTurnRule::Endpoints`, `KernelOptions` stays opt-in, no kernel or test change. Supported: the rule is safe (funnel tail mass at the paper tuning, Eight Schools 1.08x) and the right opt-in for kidiq/garch/nes-like targets. Also found: the per-gradient gap to CmdStan on healthy models is not the U-turn rule (0.434x -> 0.462x), and the sampler-default funnel is biased under every U-turn rule (z −3 to −11 over three seeds), a `Tuning::default()`-on-funnels finding that `freeze_mode_v1`'s one seed did not resolve.
- Next decision: (1) the dual-averaging step collapse on `sblrc` / `earnings` / `diamonds` (`h ~ 0.003`, hundreds of depth-10 caps, where the stan-style preset gets 2–10x per gradient) is now the largest per-gradient item; (2) the sampler-default funnel bias (four levels, `h0 = 0.5`) needs its own study before any funnel claim at the defaults — the documented funnel configuration remains the paper tuning; (3) per-model heterogeneity of the U-turn rule (gains where orbits lengthen on the 66-d GP and the HMM, losses on the depth-capped regressions) suggests the rule choice is a target property, not a default.

### WP31-JOINT-DEFAULT-V1 — `UTurnRule::MomentumSum` + `DiagonalMetricRegularization::Stan` as the `sampler` defaults: 1.51x geomean, 41 vs 35 gates, funnel and Eight Schools safe, but the preregistered "no model < 0.85x" rule fails on the two fail-everywhere cells; no flip, recommended opt-in
- Ordered time: 2026-09-03 on `wt/joint-default`; preregistration committed (`d2382b2`) before the first evidence cell; 204 posteriordb cells 11:27–14:11 local, then 24 funnel and 12 Eight Schools cells, one continuous strictly sequential run; analysis immediately after.
- Protocol/config: `STUDIES/joint_default_v1/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). The `posteriordb_bench_v3` protocol (17 posteriors `28f8d3d`, 4 chains, 1,000/1,000, `Tuning::default()` with eight levels, `Metric::diagonal()`, dual averaging 0.8 with the WP24 warmup rule, `Init::uniform()` starts shared across arms per seed, v3 gates and ArviZ estimators, models compiled fresh without `STAN_THREADS`) with four oWALNUTS arms differing only in `KernelOptions::u_turn` and the diagonal-metric regularisation: `da` (`Endpoints` + `TowardUnit`, the defaults), `rhosum` (`MomentumSum`), `stanreg` (`Stan`), `joint` (both; decided). CmdStan and nutpie cited from v3. Side checks: Neal's funnel 4 x 2,000/20,000 at the paper tuning and at the sampler defaults, three seeds, per-seed MCSE z; the `eight_schools_v9_rebench_v1` strict track, three seeds x three repetitions. Decision rule on `joint`: geomean min-bulk-ESS/gradient >= 1.15x `da` over 17 models (C1), no model < 0.85x (C2), gates >= `da` (C3), funnel |z| <= 2 on every seed at both tunings (C4), Eight Schools >= 0.9x (C5). Predictions: >= 1.2x on earnings/kidiq/sblrc/mesquite/nes2000 (P1), earnings gate recovered (P2), sblrc >= 5x (P3).
- Seeds: 86101, 86102, 86103 (verified unused; the task's 85101–85103 were consumed by `funnel_bias_fix_v1`). Deviations: none; no restart; the four `lotka_volterra` 86103 cells took 26–44 min each (frozen `rk45`-boundary chain in every arm), inside the timeout. Shared machine (`wt/posteriordb-v4` active): walls are upper bounds.
- Status: preregistered decision study; all 240 cells present; no cell errored.
- Outcome: **C1 held — 1.508** (rhosum alone 1.116, stanreg alone 1.257); **C2 not held — `hmm_drive_0` 0.005** (per seed 0.00 / 1.38 / 0.60: `joint` drew a second-mode chain on two seeds, `da` on one; the arm changes the first warmup transitions and so the mode draw) **and centered eight schools 0.79** (0/3 in every arm; `joint` has the most ESS and lowest R-hat of the four arms at 1.5x the gradients); **C3 held — 41 vs 35** (gains `earnings` x2, `sblrc` x3, `lotka_volterra` 86102, `one_comp` 86103; loses `hmm_drive_0` 86101); **C4 held** — paper +0.68/+0.61/+0.05, defaults −0.06/+0.81/−1.16, zero divergences at the defaults on every seed, `omega` R-hat 1.003–1.010 vs the default's 1.007–1.044; **C5 held — 1.294** (rhosum 1.346, stanreg 1.095). P1 held on four of five (earnings 3.69, kidiq 1.98, sblrc 9.12, nes2000 1.50; mesquite 1.12), P2 held (earnings gates da 1/3, stanreg 0/3, joint 3/3), P3 held (9.12). Per model joint/da: earnings 3.69, sblrc 9.12, arma11 2.26, one_comp 2.16, kidiq 1.98, hmm_example 1.87, nes2000 1.50, garch 1.16, mesquite 1.12, arK 1.07, noncentered 1.04, gp_pois_regr 0.98, diamonds 0.94, centered 0.79, hmm_drive_0 0.005, lotka_volterra 72 (da's 86102 bad chain absent), accel_gp 3.35 (fails everywhere). Gradients geomean 0.96x, ESS/s 1.56x. vs CmdStan (v3 medians): da 0.317x, joint 0.477x; on the healthy regressions joint 0.81/0.93/0.81/0.88/0.99x (earnings/kidiq/sblrc/nes2000/mesquite) against da's 0.22/0.47/0.09/0.58/0.88. Mechanism of the interaction: `stanreg` alone on `earnings` is 0.08x and 0/3 (R-hat 1.06–1.64) because under the endpoint rule at the corrected metric the short orbits leave the window variance at the `Stan` floor (installed mass 9.5e4–9.8e4 = 1e-5 variance) so the metric overshoots 1e2 and one chain per seed runs at 10x the others' step; under `MomentumSum` the window measures the variance and installs 5.8e3–6.2e3 = CmdStan's 1.6e-4. Default arm reproduces v3 within seed noise on twelve healthy models (0.93–1.15).
- Diagnostics: `artifacts/results-table.md` (per-model cells with final steps, decision table with per-seed ratios, funnel and Eight Schools tables, criteria and predictions), `artifacts/summary.json` (per-cell metrics including min/max installed mass), `artifacts/cells/*.json`, `artifacts/funnel/*.json` (per-chain tail mass, batch means, installed mass), `artifacts/eight_schools/*.json`; raw draws hashed, uncommitted.
- Artifacts: `STUDIES/joint_default_v1/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, src/arms.rs, src/bin/funnel.rs, src/bin/eight_schools.rs, run_joint.py, checksums.py, .gitignore, CHECKSUMS.sha256, artifacts/{summary.json, results-table.md, cells/, funnel/, eight_schools/, measured_on.json, run-log.txt, run-log.err.txt}}`; README opt-in paragraph and `wiki/release-0.2.0.md` section (documentation only).
- Conclusion: supported — the two options are one finding (WP27 + WP30): together they are 1.51x the default per gradient, pass six more cells, recover `earnings` (3/3 at zero depth caps), fix `sblrc` (9x) and the `arma11` floor (2.3x), preserve the funnel tail at both tunings with zero divergences, and are 1.29x on the Eight Schools strict track; either alone is a wash (`rhosum` 1.12x) or unstable (`stanreg` 0.08x on `earnings`). Not supported: the flip under the preregistered rule — C2 fails on `hmm_drive_0` (mode lottery, arm-dependent) and the centered eight schools (fails every arm), and the rule had no carve-out, so the defaults stay and the pair is documented as the recommended opt-in, to be used together.
- Next decision: (1) re-decide the pair on a fresh preregistration whose C2 is over models the default arm passes at least once (or over seed-paired cells with the same mode assignment) — the evidence here says it passes; (2) `hmm_drive_0`'s second-mode draw is a start/warmup property that every kernel change perturbs; a cross-chain mode check at the end of warmup (the `ChainDisagreement` diagnostic) is the right instrument before it is counted against any option; (3) `diamonds` at depth 10 is capped in every arm at `h` 0.003 and is not a floor case — the depth cap itself is the next item there; (4) the `Stan` regularisation should not be offered alone (feedback collapse at eight levels on `earnings`).

### WP32-POSTERIORDB-BENCH-V5 — the post-hoc default change (`MomentumSum` + Stan regularisation) validated on fresh seeds against CmdStan and nutpie: 42/51 vs 36 and 28, 0.82x CmdStan per gradient on the healthy models (1.07x over 17 with `arma11`), 3.09x nutpie ESS/s, funnel exact; all five predictions held
- Ordered time: 2026-09-03 on `wt/joint-flip`; the default change committed first (`d2b9733`, "DEFAULT CHANGE (post-hoc after WP31)"), then the preregistration (`b72af28`), then 153 posteriordb cells 14:32–15:02 local in one strictly sequential pass and the three funnel cells; analysis immediately after.
- Protocol/config: `STUDIES/posteriordb_bench_v5/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). The `posteriordb_bench_v3` protocol verbatim (17 posteriors `28f8d3d`, 4 chains, 1,000/1,000, v3 gates and ArviZ 0.23.4 estimators, `Init::uniform()` starts, 2,700 s cell timeout) with arms `owalnuts-da` = the shipped defaults after the change (`Tuning::default()` with `DEFAULT_U_TURN_RULE = MomentumSum`, `Adaptation::default()` with `DEFAULT_METRIC_REGULARIZATION = Stan`, `h0 0.5`, depth 10, eight levels, `delta 1`, WP24 warmup rule, adapted diagonal metric, cached initial evaluation, `Limits::admit_worst_case()`, four threads on a non-`STAN_THREADS` BridgeStan 2.9.0 build), `cmdstan` 2.39.0 defaults (cmdstanpy 1.3.0), `nutpie` 0.16.8 defaults — both rerun, not cited. Funnel row: Neal's 10-D funnel 4 x 2,000/20,000 at the sampler defaults, per-seed MCSE z. Compiled models and posteriordb checkout copied from `wt/posteriordb-v4` (not modified there); `accel_gp` compiled here. Predictions fixed before the first cell: P1 DA >= 39/51 gates; P2 geomean min-bulk-ESS/gradient vs CmdStan >= 0.45 over 17; P3 geomean ESS/s vs nutpie >= 1.5; P4 wall/gradient <= 1.0x CmdStan; P5 funnel |z| <= 2 on every seed.
- Seeds: 87101, 87102, 87103 (verified unused). Deviations: none; no restart; nutpie panicked inside its own sampler on `lotka_volterra` on every seed as in v3 (three `timeout_or_crash` cells; nutpie head-to-head over 16 models). Machine otherwise idle.
- Status: preregistered validation of a post-hoc decision; the decision was taken before this study on WP31's evidence and is not made by it. All 156 cells present; no oWALNUTS cell errored, froze, diverged or hit the depth cap outside `diamonds` (246–539 per seed) and one `accel_gp` transition.
- Outcome: **P1 held — 42/51** (CmdStan 36, nutpie 28; v3 on the old defaults 35 / 37 / 31; 12 models 3/3 against CmdStan's 10 and nutpie's 8); **P2 held — 1.069x** CmdStan per gradient over all 17 (0.822x over the 16 without `arma11`, where CmdStan lost two of three seeds to a chain that never left its start, step 0, R-hat 1.5, and oWALNUTS is 71x; v3 0.344x); **P3 held — 3.085x** nutpie ESS/s over 16 (0.841x per gradient); **P4 held — 0.801x** wall per gradient (ESS/s 1.401x CmdStan); **P5 held** — funnel +1.02 / −0.05 / +0.93 (0.0571 / 0.0474 / 0.0578 vs 0.0478), zero divergences, one chain on 87103 at `h` 0.0013 with 933 depth caps (unbiased, not efficient — WP28/WP31 caveat unchanged). DA v5/v3 geomean per gradient **2.01x** at 1.02x the gradients (2.31x ESS/s): earnings 3.28, sblrc 13.9 (0/3 -> 3/3, `h` 0.003 -> 0.10–0.12 = CmdStan's, installed max mass 9.2e4 on every chain), kidiq 1.73, nes2000 1.70, hmm_example 1.59, accel_gp 1.71, centered 1.51, garch 1.28, diamonds 1.23, mesquite 1.19, arK 1.13, one_comp 1.12, arma11 1.02, gp_pois_regr 1.03, lotka_volterra 0.95, noncentered 0.85, hmm_drive_0 134 (v3 drew two second-mode seeds, v5 one); no model below 0.8x. Competitor seed-noise control v5/v3: CmdStan 0.65x (arma11 0.003x, centered eight schools 0.28x; 0.90–1.19x on the other fifteen), nutpie 0.74x. Per model vs CmdStan per gradient: healthy models 0.67–0.95x (earnings 0.67, hmm_example 0.73, gp_pois_regr 0.75, noncentered 0.75, arK 0.81, diamonds 0.84, nes2000 0.85, garch 0.86, sblrc 0.87, lotka_volterra 0.88, mesquite 0.90, kidiq 0.92, hmm_drive_0 0.94, one_comp 0.95), centered 1.18, accel_gp 0.47, arma11 71. Outright wins (gates >=, ESS/grad >, ESS/s >): vs CmdStan `arma11` (3/3 vs 1/3) and the centered eight schools (0/3 both, more ESS at zero divergences vs 43–280); vs nutpie `diamonds` (3/3 vs 0/3), `arma11` (3/3 vs 1/3, nutpie 477–515 divergences), `gp_pois_regr` (3/3 vs 0/3). Gate wins without throughput: noncentered eight schools (3/3 vs nutpie 0/3), gp_pois_regr vs CmdStan (3/3 vs 0/3, 6–22 divergences), sblrc vs CmdStan (3/3 vs 2/3), one_comp vs nutpie (1/3 vs 0/3). ESS/s above CmdStan on 8/17 models, above nutpie on 14/16. Regressions relative to v3: none beyond seed noise (noncentered eight schools 0.85x, lotka_volterra 0.95x). `hmm_drive_0` second-mode draw: oWALNUTS one seed (87102, R-hat 1.35), CmdStan one seed (87101, R-hat 1.22, 414 divergences), nutpie none.
- Diagnostics: `artifacts/results-table.md` (per-model cells, head-to-head, per-seed final steps and depth caps, v5-vs-v3 per model and arm, DA against the v3 competitor medians, funnel table, predictions), `artifacts/summary.json` (per-cell metrics including installed-mass extremes, `u_turn` / `metric_regularization` recorded per cell), `artifacts/cells/*.json`, `artifacts/funnel/*.json`, `artifacts/measured_on.json`, `artifacts/run-log.txt`; raw draws and CmdStan CSVs hashed, uncommitted.
- Artifacts: `STUDIES/posteriordb_bench_v5/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, src/bin/funnel.rs, run_posteriordb.py, checksums.py, .gitignore, CHECKSUMS.sha256, artifacts/{summary.json, results-table.md, cells/, funnel/, measured_on.json, run-log.txt, run-log.err.txt}}`; the default change in `src/sampler.rs` (`DEFAULT_U_TURN_RULE`, `DEFAULT_METRIC_REGULARIZATION`, `Tuning::default()`, `Adaptation::warmup_config`), `tests/sampler_api.rs` pins, README, CHANGELOG, `wiki/release-0.2.0.md`, Python package docs.
- Conclusion: supported — the post-hoc pair holds up on fresh seeds against rerun competitors: the most gates of any arm in any posteriordb study, above both competitors on the same seeds, funnel exact, ESS/s above CmdStan and nutpie, no v3 regression. Not supported: a per-gradient win over CmdStan on ordinary posteriors — the honest figure is 0.82x (0.67–0.95x per healthy model; the 1.07x over 17 is `arma11`'s crawl start hitting CmdStan and nutpie on two seeds each); the kernel-side residual of WP30 remains. The decision was post hoc and is documented as such in the CHANGELOG, README and release notes; this study is its validation, not its justification.
- Next decision: (1) the per-gradient residual is now the kernel (WP30's reverse-coarser stops on refined leaves and the remaining orbit length), not warmup — a preregistered kernel study, not another warmup option; (2) `diamonds` depth caps (246–539 per seed at `h` 0.003–0.005) are the one place the depth-10 cap still binds, unchanged by the pair; (3) the funnel's one-chain step collapse at the defaults (87103 `h` 0.0013) is the WP28 item, still open; (4) the Python extension should follow the sampler defaults in its next build (it configures the facade directly and keeps `Endpoints` / `TowardUnit`, documented).
### WP29-POSTERIORDB-BENCH-V4 — the v3 protocol on the eight-level default plus a Stan-regularisation arm: eight levels free, Stan regularisation alone fails the default rule on five lost gates, driver died at 187/204 cells (superseded by WP32)
- Ordered time: 2026-09-02, preregistration committed (`74ad37d`) before the first evidence cell; 187 cells 20:32–21:26 local, one strictly sequential run that died during `lotka_volterra` cmdstan 83102 (see deviations); analysis 2026-09-03 on the cells present.
- Protocol/config: `STUDIES/posteriordb_bench_v4/{PREREGISTRATION.md, protocol.json}` (hashes in the study `CHECKSUMS.sha256`). Identical to WP25 (v3): 17 posteriordb posteriors `28f8d3d`, 4 chains, 1,000/1,000, every sampler at its defaults, v3 gates and estimators; differences: harness rebuilt against `src/` after the WP26/27/28 merges so `Tuning::default()` has `max_refinement_levels = 8` (kernel revision v10 unchanged; every cell records the 8), arms owalnuts-da, **owalnuts-da-stanreg** (`Adaptation::Custom(WarmupConfig::new(0.8).with_warmup_exhaustion_rule(DEFAULT_WARMUP_EXHAUSTION).with_metric_regularization(DiagonalMetricRegularization::Stan))`, the WP27 `reg` arm), cmdstan 2.39.0, nutpie 0.16.8 (stan-style control dropped), every model compiled fresh; per-cell fraction of retained transitions at refinement level > 0 and at levels 5–8 recorded.
- Seeds: 83101, 83102, 83103 (verified unused on every branch). Deviations: the driver died after 187 of 204 cells (last cell `lotka_volterra` cmdstan 83101 at 21:25:54; the launching background shell was reported killed at ~21:32 while cmdstan 83102 ran a chain on the `rk45` boundary); not relaunched — superseded by WP32 (v5) — and finalised on the cells present at the coordinator's direction. Missing: `lotka_volterra` cmdstan 83102–83103 and nutpie 83101–83103, all twelve `accel_gp` cells, marked *not run*.
- Status: preregistered; incomplete (187/204); no oWALNUTS cell errored; superseded by WP32.
- Outcome: cells passing all gates — owalnuts-da **39**/48 (12 models 3/3; v3 35/51), owalnuts-da-stanreg 33/48, cmdstan 34/46, nutpie 28/45. **Eight levels are free**: levels 5–8 selected by 0.000 of retained transitions on 15 of 16 measured models and 0.005 on the centered eight schools; any refinement 0.3–8 % (centered eight schools 25–29 %); DA v4/v3 ESS/gradient **0.999** geomean on the 13 models without a seed-pathological draw (CmdStan's own 0.952), 0.515x CmdStan on the 12 non-pathological models (v3 0.498x), **0.694x wall per gradient**, 1.67x nutpie per second (9/15 models). Headline DA vs CmdStan over the 15 complete models is **0.845x** per gradient only because CmdStan crawled on two `arma11` seeds (`h` 6e-8 / 2e-7, R-hat 1.59, min ESS 7 — the WP27 §1b start; 1,296x on that model); 0.500x without it (v3 on the same 14: 0.335x, the gap being `hmm_drive_0`'s mode draw: v3 two second-mode seeds, v4 none, 83.6x). Seed draws: `lotka_volterra` 83102 froze one chain at `h` 3e-5 (996 exhaustions) in both oWALNUTS arms; `arma11` 83103 has a slow chain at `h` 0.016 (3/3 anyway). **Stan regularisation alone**: `sblrc` **7.76x** (`h` 0.10 vs 0.004, 3/3 from 0/3), `arma11` 2.48x, hmm_example 1.75x, geomean **1.158x** DA — but `earnings` **0.35x** (0/3, R-hat 1.02–1.14, 0.12x gradients), `diamonds` 0.71x (2/3), and five models lose gates (`earnings` 3->0, `kidiq` 3->0 at R-hat 1.012–1.019 and 0.47x gradients, `diamonds` 3->2, `hmm_drive_0` 3->2 second-mode chain, `one_comp` 1->0): 33 vs 39 cells. Predictions: P1 (>= 35: 39) and P5 (wall/grad <= 1.0x: 0.694) held; P2 (0.9–1.1x v3's 0.344: 0.845) not held by the letter, held on matched sets; P3 (stanreg >= 2x on sblrc and earnings) half — sblrc 7.76, earnings 0.35; P4 (>= 1.1x, none < 0.8x) not held on earnings and diamonds. Default rule ">= 1.1x geomean, no model < 0.8x, no gate lost": **not met**. Agreement: no oWALNUTS cell flagged; CmdStan two flags and nutpie one, all on failing cells.
- Diagnostics: `artifacts/results-table.md` (per-model table with refined fraction and depth caps, head-to-head, stanreg-vs-DA per model with final steps, v3-vs-v4 per model, refinement table, predictions), `artifacts/summary.json`, `artifacts/cells/*.json` (per-parameter z, per-chain steps/exhaustions, depth and nine-bin refinement histograms, the recorded regularisation), raw draws hashed and uncommitted.
- Artifacts: `STUDIES/posteriordb_bench_v4/{PREREGISTRATION.md, protocol.json, README.md, LEDGER-ENTRY.md, Cargo.toml, Cargo.lock, src/main.rs, run_posteriordb.py, checksums.py, CHECKSUMS.sha256, artifacts/{summary.json, results-table.md, cells/, measured_on.json, run-log.txt, run-log.err.txt}}`.
- Conclusion: supported — (a) the WP28 eight-level default costs nothing on the posteriordb set (the extra levels are never selected outside the centered eight schools; the DA arm reproduces v3 within seed noise and passes more cells); (b) `DiagonalMetricRegularization::Stan` fixes the `sblrc` collapse and the `arma11` unit-floor step on the full set, as WP27 measured. Not supported: the regularisation as a default on its own — at the corrected metric the endpoint-U-turn orbit is too short on `earnings`, `kidiq` and `diamonds` (R-hat misses at a fraction of the gradients), so the metric fix needs the orbit fix; that joint candidate is what WP32 (v5) tested. Not decided here: a headline against CmdStan — 15 of 17 models, and CmdStan's own `arma11` crawls make the all-model geomean uninformative this round.
- Next decision: superseded by WP32. What v4 adds to it: the per-model list of where the regularisation alone loses gates (`earnings`, `kidiq`, `diamonds`, `hmm_drive_0`, `one_comp`) is the regression set any joint default must clear; the `lotka_volterra` `rk45`-boundary freeze (83102, both arms) and the `arma11` start that CmdStan also crawls on remain start-rule matters outside warmup.
